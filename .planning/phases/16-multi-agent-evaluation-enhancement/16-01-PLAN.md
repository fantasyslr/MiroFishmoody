---
phase: 16-multi-agent-evaluation-enhancement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config/personas/moodyplus.json
  - backend/app/config/personas/colored_lenses.json
  - backend/app/services/pairwise_judge.py
  - backend/tests/test_persona_registry.py
  - backend/tests/test_pairwise_judge_multijudge.py
autonomous: true
requirements:
  - MA-03
  - MA-04

must_haves:
  truths:
    - "moodyPlus Evaluate 推演调用 9 个人格（PersonaRegistry 加载 moodyplus.json 返回 9 条）"
    - "colored_lenses Evaluate 推演调用 8 个人格（PersonaRegistry 加载 colored_lenses.json 返回 8 条）"
    - "每对方案的 Pairwise judge 轮次中，奇数 judge index 收到 (A,B) 顺序、偶数 judge index 收到 (B,A) 顺序"
    - "所有 judge 投票结果（含反序）都出现在 PairwiseResult.votes 中"
  artifacts:
    - path: "backend/app/config/personas/moodyplus.json"
      provides: "9 persona definitions for moodyPlus category"
      contains: "\"id\": \"tech_perceiver\""
    - path: "backend/app/config/personas/colored_lenses.json"
      provides: "8 persona definitions for colored_lenses category"
      contains: "\"id\": \"beauty_blogger\""
    - path: "backend/app/services/pairwise_judge.py"
      provides: "MultiJudge position-alternating ensemble"
      exports: ["MultiJudgeEnsemble"]
  key_links:
    - from: "backend/app/services/evaluation_orchestrator.py"
      to: "backend/app/services/pairwise_judge.py"
      via: "judge = PairwiseJudge(...) or MultiJudgeEnsemble(...)"
      pattern: "MultiJudgeEnsemble|PairwiseJudge"
    - from: "backend/app/services/audience_panel.py"
      to: "backend/app/config/personas/moodyplus.json"
      via: "PersonaRegistry.get_personas(category='moodyplus')"
      pattern: "get_personas"
---

<objective>
扩展人格池（moodyPlus 6→9, colored_lenses 5→8），并为 PairwiseJudge 引入 MultiJudge ensemble，强制奇偶交替呈现顺序消除 position bias。

Purpose: 更大的人格池让评审覆盖更多用户维度；强制位置交替确保 judge 的每一票都代表真实偏好而非顺序偏见。
Output: 扩展后的 persona JSON 文件 + MultiJudgeEnsemble 类 + 对应测试。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-multi-agent-evaluation-enhancement/16-CONTEXT.md

<interfaces>
<!-- Key interfaces executor needs. No codebase exploration required. -->

From backend/app/config/personas/moodyplus.json (current — 6 personas):
```json
[
  { "id": "daily_wearer", "name": "日抛隐形眼镜老用户", "description": "...", "evaluation_focus": "..." },
  { "id": "acuvue_switcher", "name": "Acuvue Define 现有用户", ... },
  { "id": "eye_health", "name": "眼健康关注者", ... },
  { "id": "office_comfort", "name": "办公室长时间佩戴用户", ... },
  { "id": "active_lifestyle", "name": "运动爱好者", ... },
  { "id": "sensitive_eyes", "name": "敏感眼用户", ... }
]
```

From backend/app/config/personas/colored_lenses.json (current — 5 personas):
```json
[
  { "id": "beauty_first", "name": "美瞳新用户（颜值驱动）", ... },
  { "id": "price_conscious", "name": "价格敏感用户", ... },
  { "id": "makeup_influencer", "name": "美妆博主", ... },
  { "id": "cosplay_occasion", "name": "Coser/特殊场合用户", ... },
  { "id": "natural_enhancer", "name": "自然放大需求用户", ... }
]
```

PersonaRegistry required fields (all must be non-empty strings):
  "id", "name", "description", "evaluation_focus"

From backend/app/services/pairwise_judge.py:
```python
JUDGE_PERSPECTIVES = [
    {"id": "strategist", "name": "策略视角", "system_prompt": "..."},
    {"id": "consumer", "name": "用户视角", "system_prompt": "..."},
    {"id": "brand_guardian", "name": "品牌视角", "system_prompt": "..."},
]

class PairwiseJudge:
    def evaluate_pair(self, a: Campaign, b: Campaign) -> PairwiseResult:
        # Calls 3 judges in JUDGE_PERSPECTIVES, each in both (A,B) and (B,A) order
        # Already has _flip_vote() for position debiasing
        ...

    def evaluate_all(self, campaigns: List[Campaign], max_workers: int = 4) -> Tuple[List[PairwiseResult], Dict[str, float]]:
        # Runs all pairs via ThreadPoolExecutor
        ...
```

From backend/app/models/evaluation.py:
```python
@dataclass
class PairwiseResult:
    campaign_a_id: str
    campaign_b_id: str
    winner_id: Optional[str]
    votes: List[Dict[str, Any]]
    dimensions: Dict[str, str]
    position_swap_consistent: bool = True
    swap_votes: List[Dict[str, Any]] = field(default_factory=list)
```

EvaluationOrchestrator uses (backend/app/services/evaluation_orchestrator.py line 94):
```python
judge = PairwiseJudge(llm_client=llm)
pairwise_results, bt_scores = judge.evaluate_all(campaigns)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 moodyPlus 和 colored_lenses 人格池</name>
  <files>
    backend/app/config/personas/moodyplus.json,
    backend/app/config/personas/colored_lenses.json,
    backend/tests/test_persona_registry.py
  </files>
  <read_first>
    backend/app/config/personas/moodyplus.json,
    backend/app/config/personas/colored_lenses.json,
    backend/app/services/persona_registry.py,
    backend/tests/test_persona_registry.py (if exists)
  </read_first>
  <behavior>
    - PersonaRegistry.get_personas(category='moodyplus') returns list of length 9
    - PersonaRegistry.get_personas(category='colored_lenses') returns list of length 8
    - All new personas pass _validate_persona() (id, name, description, evaluation_focus all non-empty strings)
    - New moodyPlus persona IDs: tech_perceiver, medical_compliance, daily_comfort_user
    - New colored_lenses persona IDs: beauty_blogger, visual_creator, subculture_fan
  </behavior>
  <action>
    RED: Add tests to backend/tests/test_persona_registry.py asserting len==9 for moodyplus and len==8 for colored_lenses.

    GREEN: Append 3 new persona objects to moodyplus.json (keep existing 6 intact):
    1. id: "tech_perceiver", name: "科技感知者" — 对隐形眼镜材料科学有兴趣的男性用户（30岁），关注新型硅水凝胶技术参数、透氧率数字、与竞品的技术对比。会读产品白皮书，对"临床数据"敏感。evaluation_focus: "技术创新点是否有数据支撑、与竞品参数差异是否清晰"
    2. id: "medical_compliance", name: "医疗合规顾问" — 从事医疗器械合规工作的专业人士（35岁女性），关注产品声称是否有认证依据，讨厌夸大宣传，会盯着"临床级"等措辞。evaluation_focus: "合规性声称是否有依据、是否存在误导性用语"
    3. id: "daily_comfort_user", name: "日常佩戴体验者" — 32岁全职妈妈，每天佩戴透明片接送孩子、处理家务。关注全天舒适度，对价格中等敏感，优先考虑不折腾眼睛。evaluation_focus: "全天佩戴实际体验感、适合忙碌生活方式的便利性"

    Append 3 new persona objects to colored_lenses.json (keep existing 5 intact):
    1. id: "beauty_blogger", name: "彩妆博主" — 与现有 makeup_influencer 不同，此角色更偏向专业彩妆师视角（25岁），关注美瞳与彩妆的整体造型配合、不同肤色的适配性。evaluation_focus: "与彩妆产品的视觉协调度、造型可塑性"
    2. id: "visual_creator", name: "摄影师/视觉创作者" — 28岁摄影师，关注美瞳在不同光线条件下的显色稳定性、是否适合专业摄影场景出片。evaluation_focus: "多光线场景下的显色效果、是否适合高清镜头"
    3. id: "subculture_fan", name: "亚文化爱好者" — 20岁二次元/街头文化爱好者，寻求彩片表达个性和身份认同，关注独特花色和小众设计。evaluation_focus: "个性化和独特性是否足够、亚文化圈的认同度"

    All new personas must follow exact JSON schema with 4 required string fields.
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_persona_registry.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"id"' backend/app/config/personas/moodyplus.json outputs 9
    - grep -c '"id"' backend/app/config/personas/colored_lenses.json outputs 8
    - uv run pytest tests/test_persona_registry.py passes
  </acceptance_criteria>
  <done>moodyplus.json 含 9 条 persona，colored_lenses.json 含 8 条，测试绿灯</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MultiJudge ensemble — 强制奇偶位置交替</name>
  <files>
    backend/app/services/pairwise_judge.py,
    backend/tests/test_pairwise_judge_multijudge.py
  </files>
  <read_first>
    backend/app/services/pairwise_judge.py,
    backend/app/models/evaluation.py
  </read_first>
  <behavior>
    - MultiJudgeEnsemble(judges=5) creates 5 judge slots
    - For judge at index i: if i % 2 == 0 → evaluates (A, B); if i % 2 == 1 → evaluates (B, A) then flips
    - All votes (normal + swapped-and-flipped) are collected in PairwiseResult.votes
    - Final winner = majority across all collected votes (after label normalization)
    - MultiJudgeEnsemble.evaluate_all() has same signature as PairwiseJudge.evaluate_all()
    - MultiJudgeEnsemble is a drop-in replacement: accepts same (campaigns, max_workers) args
  </behavior>
  <action>
    RED: Create backend/tests/test_pairwise_judge_multijudge.py with unit tests:
    - test_alternating_order: mock judge_pair, verify call args alternate (A,B) and (B,A)
    - test_vote_count: with N judges, result.votes has N entries (after flip normalization)
    - test_majority_winner: mock 3 votes for A, 2 for B → winner is A
    - test_same_signature: MultiJudgeEnsemble().evaluate_all accepts (campaigns, max_workers=4)

    GREEN: Add MultiJudgeEnsemble class to backend/app/services/pairwise_judge.py (append at end of file, do NOT modify existing PairwiseJudge class):

    ```python
    class MultiJudgeEnsemble(PairwiseJudge):
        """Position-alternating multi-judge ensemble.

        Judges at even indices evaluate (A, B); odd indices evaluate (B, A) then flip.
        All normalized votes collected in PairwiseResult.votes.
        """

        def __init__(self, llm_client: LLMClient = None, num_judges: int = None):
            super().__init__(llm_client=llm_client)
            # Default: repeat JUDGE_PERSPECTIVES twice (6 judges) for 3-normal + 3-swapped
            self._num_judges = num_judges or (len(JUDGE_PERSPECTIVES) * 2)

        def evaluate_pair(self, a: Campaign, b: Campaign) -> PairwiseResult:
            """Alternate (A,B) and (B,A) across judge slots. All votes normalized to A/B labels."""
            perspectives = JUDGE_PERSPECTIVES * ((self._num_judges // len(JUDGE_PERSPECTIVES)) + 1)
            perspectives = perspectives[:self._num_judges]

            all_votes = []
            for idx, judge in enumerate(perspectives):
                if idx % 2 == 0:
                    # Normal order: A vs B
                    try:
                        vote = self._safe_judge(a, b, judge)
                        vote["position"] = "normal"
                        all_votes.append(vote)
                    except Exception as e:
                        logger.error(f"MultiJudge[{idx}] normal {a.id} vs {b.id}: {e}")
                else:
                    # Swapped order: B vs A, then flip labels back
                    try:
                        raw = self._safe_judge(b, a, judge)
                        vote = self._flip_vote(raw)
                        vote["position"] = "swapped"
                        all_votes.append(vote)
                    except Exception as e:
                        logger.error(f"MultiJudge[{idx}] swapped {b.id} vs {a.id}: {e}")

            # Majority across all normalized votes
            a_wins = sum(1 for v in all_votes if v["winner"] == "A")
            b_wins = sum(1 for v in all_votes if v["winner"] == "B")

            if a_wins > b_wins:
                winner_id = a.id
                majority = "A"
            elif b_wins > a_wins:
                winner_id = b.id
                majority = "B"
            else:
                winner_id = None
                majority = "tie"

            # Dimension consensus
            merged_dims = {}
            for dim in DIMENSIONS:
                dim_votes = [v["dimensions"].get(dim, "tie") for v in all_votes]
                a_count = dim_votes.count("A")
                b_count = dim_votes.count("B")
                merged_dims[dim] = a.id if a_count > b_count else (b.id if b_count > a_count else "tie")

            # position_swap_consistent: check normal vs swapped sub-majority
            normal_votes = [v for v in all_votes if v.get("position") == "normal"]
            swap_votes = [v for v in all_votes if v.get("position") == "swapped"]
            normal_a = sum(1 for v in normal_votes if v["winner"] == "A")
            normal_b = sum(1 for v in normal_votes if v["winner"] == "B")
            swap_a = sum(1 for v in swap_votes if v["winner"] == "A")
            swap_b = sum(1 for v in swap_votes if v["winner"] == "B")
            normal_majority = "A" if normal_a > normal_b else ("B" if normal_b > normal_a else "tie")
            swap_majority = "A" if swap_a > swap_b else ("B" if swap_b > swap_a else "tie")
            position_swap_consistent = (normal_majority == swap_majority)

            return PairwiseResult(
                campaign_a_id=a.id,
                campaign_b_id=b.id,
                winner_id=winner_id,
                votes=all_votes,
                dimensions=merged_dims,
                position_swap_consistent=position_swap_consistent,
                swap_votes=swap_votes,
            )
    ```

    Also update EvaluationOrchestrator (backend/app/services/evaluation_orchestrator.py) to use MultiJudgeEnsemble by default:
    - Add import: `from ..services.pairwise_judge import PairwiseJudge, MultiJudgeEnsemble`
    - Replace `judge = PairwiseJudge(llm_client=llm)` with `judge = MultiJudgeEnsemble(llm_client=llm)`
    - Keep the `if Config.USE_MARKET_JUDGE:` block unchanged above (it still uses MarketJudge when flag set)
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_pairwise_judge_multijudge.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "class MultiJudgeEnsemble" backend/app/services/pairwise_judge.py returns match
    - grep "MultiJudgeEnsemble" backend/app/services/evaluation_orchestrator.py returns match
    - uv run pytest tests/test_pairwise_judge_multijudge.py passes
    - uv run pytest passes (all existing tests still green)
  </acceptance_criteria>
  <done>MultiJudgeEnsemble 类存在，奇偶交替逻辑覆盖，EvaluationOrchestrator 默认使用它，全套测试绿灯</done>
</task>

</tasks>

<verification>
cd /Users/slr/MiroFishmoody/backend && uv run pytest -x -q 2>&1 | tail -10
</verification>

<success_criteria>
- moodyplus.json 包含 9 个 persona，colored_lenses.json 包含 8 个
- MultiJudgeEnsemble 类在 pairwise_judge.py 中，奇数 index judge 收到 (B,A) 顺序
- EvaluationOrchestrator 默认实例化 MultiJudgeEnsemble
- 所有现有测试 + 新测试绿灯
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-agent-evaluation-enhancement/16-01-SUMMARY.md`
</output>
