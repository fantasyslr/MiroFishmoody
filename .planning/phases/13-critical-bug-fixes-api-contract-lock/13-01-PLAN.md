---
phase: 13-critical-bug-fixes-api-contract-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/campaign.py
  - backend/tests/test_phase13_evaluate_parser.py
  - backend/tests/test_phase13_evaluate_endpoint.py
autonomous: true
requirements:
  - BUG-05

must_haves:
  truths:
    - "POST /api/campaign/evaluate 接受 {campaign_id, name, description, image_paths} 形状的 payload 并返回 200（不再 400）"
    - "_parse_evaluate_campaigns() 将 campaign_id 字段映射为 Campaign.id（保持 UUID 前缀与已上传图片文件名一致）"
    - "description 字段映射为 core_message，作为 LLM 评估上下文"
    - "image_paths 数组原样保留在 Campaign 对象中，AudiencePanel/PairwiseJudge 可正常读取"
  artifacts:
    - path: "backend/app/api/campaign.py"
      provides: "_parse_evaluate_campaigns() 函数，evaluate() 端点改用它替代 _parse_campaigns()"
      contains: "_parse_evaluate_campaigns"
    - path: "backend/tests/test_phase13_evaluate_parser.py"
      provides: "_parse_evaluate_campaigns() 单元测试"
      exports: ["test_parse_evaluate_campaigns_basic", "test_parse_evaluate_campaigns_image_paths", "test_parse_evaluate_campaigns_id_mapping"]
    - path: "backend/tests/test_phase13_evaluate_endpoint.py"
      provides: "/api/campaign/evaluate 集成测试"
      exports: ["test_evaluate_endpoint_accepts_evaluate_payload"]
  key_links:
    - from: "backend/app/api/campaign.py evaluate()"
      to: "_parse_evaluate_campaigns()"
      via: "直接调用替换 _parse_campaigns()"
      pattern: "_parse_evaluate_campaigns\\(data\\)"
    - from: "_parse_evaluate_campaigns()"
      to: "Campaign(id=campaign_id, ...)"
      via: "c.get('campaign_id') 优先于 c.get('id')"
      pattern: "c\\.get\\(\"campaign_id\"\\)"
---

<objective>
修复 BUG-05：Evaluate 模式因 payload 字段不匹配导致所有评审提交返回 400 错误。

Purpose: 当前 evaluate() 端点调用 _parse_campaigns()，该函数要求 `id` 和 `core_message` 字段，但前端发送的是 `campaign_id` 和 `description`，导致所有 Evaluate 提交 400 失败。此 bug 使视觉评估完全不可用（不是"盲评"，是"根本不运行"）。

Output:
- backend/app/api/campaign.py 中增加 _parse_evaluate_campaigns()
- evaluate() 端点改用新函数
- 两个测试文件覆盖解析逻辑和端点行为
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-critical-bug-fixes-api-contract-lock/13-RESEARCH.md

<interfaces>
<!-- 关键接口：executor 直接使用，无需探索代码库 -->

From backend/app/api/campaign.py (现有函数，不可修改):
```python
def _parse_campaigns(data: dict) -> CampaignSet:
    # 要求字段: c.get("id"), c.get("core_message") (必填非空), c.get("name") (必填)
    # 用于 /race 端点 — 不要改动此函数
```

From backend/app/models/campaign.py:
```python
@dataclass
class Campaign:
    id: str
    name: str
    product_line: ProductLine
    target_audience: str
    core_message: str
    channels: list
    creative_direction: str
    image_paths: list  # 图片 URL 列表，AudiencePanel/PairwiseJudge 通过此字段读取图片
    extra: dict
    budget_range: str | None = None
    kv_description: str | None = None
    promo_mechanic: str | None = None

class ProductLine(str, Enum):
    COLORED = "colored_lenses"
    MOODYPLUS = "moodyplus"

@dataclass
class CampaignSet:
    set_id: str
    campaigns: list[Campaign]
    context: str = ""
    created_at: str = ""
```

From frontend/src/lib/api.ts (前端发送的 EvaluatePayload 形状):
```typescript
export type EvaluatePayload = {
  set_id: string
  campaigns: Array<{
    campaign_id: string   // 对应后端 Campaign.id（UUID 前缀，与图片文件名一致）
    name: string
    description?: string  // 对应后端 core_message
    image_paths?: string[]
  }>
  category?: string
  parent_set_id?: string
}
```

From backend/app/api/campaign.py (evaluate 端点当前问题行):
```python
# line 301 — BUG: 错误调用，导致所有评审提交 400
campaign_set = _parse_campaigns(data)  # 期望 core_message，收到 description
```

From backend/app/config.py:
```python
class Config:
    MAX_CAMPAIGNS = 5
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建测试文件（Wave 0 — 先写失败测试）</name>
  <files>backend/tests/test_phase13_evaluate_parser.py, backend/tests/test_phase13_evaluate_endpoint.py</files>

  <read_first>
    - backend/tests/ 目录（了解现有测试文件命名和导入约定）
    - backend/app/api/campaign.py（确认当前 _parse_campaigns 签名和 evaluate 端点行为）
    - backend/app/models/campaign.py（Campaign/CampaignSet dataclass 字段）
  </read_first>

  <behavior>
    test_phase13_evaluate_parser.py 测试用例：
    - test_parse_evaluate_campaigns_basic: 输入 2 个 campaign（含 campaign_id、name、description），断言返回 CampaignSet，len(campaigns)==2，campaign[0].id == 输入的 campaign_id，campaign[0].core_message == 输入的 description
    - test_parse_evaluate_campaigns_image_paths: 输入带 image_paths 的 campaign，断言 campaign.image_paths 原样保留
    - test_parse_evaluate_campaigns_id_mapping: campaign_id 优先于 id 字段；若 campaign_id 存在则 Campaign.id == campaign_id（不是 "campaign_1"）
    - test_parse_evaluate_campaigns_description_fallback: description 缺失时 core_message 回退为 name（不抛异常）
    - test_parse_evaluate_campaigns_min_campaigns: 少于 2 个 campaign 时抛 ValueError

    test_phase13_evaluate_endpoint.py 测试用例（集成测试，使用 Flask test client）：
    - test_evaluate_endpoint_accepts_evaluate_payload: POST /api/campaign/evaluate 发送有效 EvaluatePayload 形状（campaign_id 而非 id，description 而非 core_message），断言响应状态码为 202（不是 400），响应包含 task_id 字段
  </behavior>

  <action>
    创建 backend/tests/test_phase13_evaluate_parser.py：

    ```python
    """Phase 13 BUG-05: _parse_evaluate_campaigns() 单元测试"""
    import pytest
    from app.api.campaign import _parse_evaluate_campaigns


    def _make_data(campaigns, set_id="test-set-001"):
        return {"set_id": set_id, "campaigns": campaigns}


    def test_parse_evaluate_campaigns_basic():
        data = _make_data([
            {"campaign_id": "uuid-aaa", "name": "方案A", "description": "核心卖点A"},
            {"campaign_id": "uuid-bbb", "name": "方案B", "description": "核心卖点B"},
        ])
        result = _parse_evaluate_campaigns(data)
        assert len(result.campaigns) == 2
        assert result.campaigns[0].id == "uuid-aaa"
        assert result.campaigns[0].core_message == "核心卖点A"
        assert result.campaigns[0].name == "方案A"


    def test_parse_evaluate_campaigns_image_paths():
        paths = ["/api/campaign/image-file/set1/uuid-aaa__x__img.jpg"]
        data = _make_data([
            {"campaign_id": "uuid-aaa", "name": "方案A", "description": "测试", "image_paths": paths},
            {"campaign_id": "uuid-bbb", "name": "方案B", "description": "测试2", "image_paths": []},
        ])
        result = _parse_evaluate_campaigns(data)
        assert result.campaigns[0].image_paths == paths


    def test_parse_evaluate_campaigns_id_mapping():
        """campaign_id 优先于 id 字段"""
        data = _make_data([
            {"campaign_id": "real-uuid", "id": "wrong-id", "name": "方案A", "description": "描述A"},
            {"campaign_id": "real-uuid-2", "name": "方案B", "description": "描述B"},
        ])
        result = _parse_evaluate_campaigns(data)
        assert result.campaigns[0].id == "real-uuid"


    def test_parse_evaluate_campaigns_description_fallback():
        """description 缺失时 core_message 回退为 name，不抛异常"""
        data = _make_data([
            {"campaign_id": "uuid-aaa", "name": "方案A"},
            {"campaign_id": "uuid-bbb", "name": "方案B"},
        ])
        result = _parse_evaluate_campaigns(data)
        assert result.campaigns[0].core_message == "方案A"


    def test_parse_evaluate_campaigns_min_campaigns():
        data = _make_data([{"campaign_id": "uuid-aaa", "name": "方案A", "description": "desc"}])
        with pytest.raises(ValueError, match="至少需要 2 个"):
            _parse_evaluate_campaigns(data)
    ```

    创建 backend/tests/test_phase13_evaluate_endpoint.py：

    ```python
    """Phase 13 BUG-05: /api/campaign/evaluate 端点集成测试"""
    import pytest
    import json


    def test_evaluate_endpoint_accepts_evaluate_payload(client, auth_headers):
        """POST /evaluate 接受 EvaluatePayload 形状（campaign_id + description），返回 202"""
        payload = {
            "set_id": "phase13-test-set-001",
            "campaigns": [
                {
                    "campaign_id": "uuid-test-001",
                    "name": "测试方案A",
                    "description": "这是方案A的核心卖点",
                    "image_paths": [],
                },
                {
                    "campaign_id": "uuid-test-002",
                    "name": "测试方案B",
                    "description": "这是方案B的核心卖点",
                    "image_paths": [],
                },
            ],
            "category": "colored_lenses",
        }
        resp = client.post(
            "/api/campaign/evaluate",
            data=json.dumps(payload),
            content_type="application/json",
            headers=auth_headers,
        )
        assert resp.status_code == 202, f"Expected 202, got {resp.status_code}: {resp.get_data(as_text=True)}"
        data = resp.get_json()
        assert "task_id" in data
        assert "set_id" in data
    ```

    注意：如果测试目录中没有 conftest.py 提供 client/auth_headers fixtures，检查现有 conftest.py 并使用相同的 fixture 名称。若不存在这些 fixtures，将集成测试标记为 pytest.mark.skip(reason="需要 conftest fixtures") 并记录。
  </action>

  <verify>
    <automated>cd /Users/slr/MiroFishmoody && uv run --project backend pytest backend/tests/test_phase13_evaluate_parser.py -x -q --no-header 2>&1 | head -20</automated>
  </verify>

  <done>
    - test_phase13_evaluate_parser.py 存在且所有 5 个测试函数定义完整
    - 运行 pytest test_phase13_evaluate_parser.py 时，因 _parse_evaluate_campaigns 不存在而 FAIL（ImportError 或 AttributeError）——这是 TDD 的 RED 阶段，预期失败
    - test_phase13_evaluate_endpoint.py 存在且 test_evaluate_endpoint_accepts_evaluate_payload 定义完整
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 _parse_evaluate_campaigns() 并接入 evaluate() 端点</name>
  <files>backend/app/api/campaign.py</files>

  <read_first>
    - backend/app/api/campaign.py（完整文件：查看 _parse_campaigns() 实现、evaluate() 端点、imports、Config 和 Campaign 的导入）
    - backend/app/models/campaign.py（Campaign dataclass 完整字段列表）
    - backend/tests/test_phase13_evaluate_parser.py（刚创建的测试，确认要满足的断言）
  </read_first>

  <behavior>
    _parse_evaluate_campaigns() 必须满足：
    - campaign_id 字段（前端 UUID）作为 Campaign.id，优先于 id 字段，不回退到 "campaign_{i+1}"
    - description 映射为 core_message；缺失时回退为 name（不抛异常）
    - image_paths 原样传入 Campaign(image_paths=...)
    - product_line 默认 "colored_lenses"（ProductLine.COLORED），无效值时同样默认（不抛异常）
    - 少于 2 个 campaign 时抛 ValueError("至少需要 2 个 campaign 方案")
    - 重复 campaign_id 时抛 ValueError
  </behavior>

  <action>
    在 backend/app/api/campaign.py 中，在 _parse_campaigns() 函数结束后（约第 121 行之后）插入新函数：

    ```python
    def _parse_evaluate_campaigns(data: dict) -> CampaignSet:
        """解析 EvaluatePayload 形状: {campaign_id, name, description, image_paths}

        与 _parse_campaigns() 的区别：
        - 接受 campaign_id（不是 id），保持与已上传图片文件名前缀一致
        - 接受 description（不是 core_message），映射为 LLM 评估上下文
        - product_line 无效值时安全回退（不抛异常）
        """
        campaigns_raw = data.get("campaigns", [])
        if not campaigns_raw or len(campaigns_raw) < 2:
            raise ValueError("至少需要 2 个 campaign 方案")
        if len(campaigns_raw) > Config.MAX_CAMPAIGNS:
            raise ValueError(f"最多支持 {Config.MAX_CAMPAIGNS} 个方案")

        campaigns = []
        seen_ids = set()
        for i, c in enumerate(campaigns_raw):
            # campaign_id 优先（前端发送 UUID，与图片文件名前缀一致）
            campaign_id = c.get("campaign_id") or c.get("id") or f"campaign_{i+1}"
            if campaign_id in seen_ids:
                raise ValueError(f"方案 {i+1}: campaign_id '{campaign_id}' 重复")
            seen_ids.add(campaign_id)

            name = c.get("name", "").strip()
            if not name:
                raise ValueError(f"方案 {i+1}: name 不能为空")

            # description -> core_message；缺失时回退为 name
            core_message = c.get("description") or c.get("core_message") or name

            product_line_str = c.get("product_line", "colored_lenses")
            try:
                product_line = ProductLine(product_line_str)
            except ValueError:
                product_line = ProductLine.COLORED  # 安全回退

            campaigns.append(Campaign(
                id=campaign_id,
                name=name,
                product_line=product_line,
                target_audience=c.get("target_audience", ""),
                core_message=core_message,
                channels=c.get("channels", []),
                creative_direction=c.get("creative_direction", ""),
                image_paths=c.get("image_paths", []),
                extra=c.get("extra", {}),
            ))

        set_id = data.get("set_id", str(uuid.uuid4()))
        return CampaignSet(set_id=set_id, campaigns=campaigns)
    ```

    然后在 evaluate() 函数内，将第 301 行的调用从：
    ```python
    campaign_set = _parse_campaigns(data)
    ```
    改为：
    ```python
    campaign_set = _parse_evaluate_campaigns(data)
    ```

    注意：不修改 _parse_campaigns() 本身（Race 路径仍使用它）。
  </action>

  <verify>
    <automated>cd /Users/slr/MiroFishmoody && uv run --project backend pytest backend/tests/test_phase13_evaluate_parser.py -x -q --no-header 2>&1 | tail -5</automated>
  </verify>

  <done>
    - backend/app/api/campaign.py 中 _parse_evaluate_campaigns 函数存在（grep 可验证：`grep -n "_parse_evaluate_campaigns" backend/app/api/campaign.py`）
    - evaluate() 端点调用 _parse_evaluate_campaigns(data)（不再是 _parse_campaigns(data)）
    - pytest test_phase13_evaluate_parser.py 5 个测试全部 GREEN
    - 全量测试套件保持 601 passed（基线不退化）：`uv run --project backend pytest backend/ -q --no-header -k "not parquet"` 通过
  </done>
</task>

</tasks>

<verification>
```bash
# 1. 解析器单元测试
cd /Users/slr/MiroFishmoody && uv run --project backend pytest backend/tests/test_phase13_evaluate_parser.py -v --no-header

# 2. 端点集成测试（若 fixtures 存在）
cd /Users/slr/MiroFishmoody && uv run --project backend pytest backend/tests/test_phase13_evaluate_endpoint.py -v --no-header

# 3. 全量回归（基线不退化）
cd /Users/slr/MiroFishmoody && uv run --project backend pytest backend/ -q --no-header -k "not parquet"

# 4. 关键修改验证
grep -n "_parse_evaluate_campaigns" /Users/slr/MiroFishmoody/backend/app/api/campaign.py
# 应有两行：函数定义行 + evaluate() 中的调用行
```
</verification>

<success_criteria>
1. _parse_evaluate_campaigns() 在 campaign.py 中存在，evaluate() 端点调用它
2. pytest test_phase13_evaluate_parser.py — 5 个测试全部 GREEN
3. grep 确认 evaluate() 中不再有 `_parse_campaigns(data)` 这行调用
4. 全量 pytest 套件（排除 parquet）不退化（>=601 passed）
</success_criteria>

<output>
完成后创建 `.planning/phases/13-critical-bug-fixes-api-contract-lock/13-01-SUMMARY.md`
</output>
