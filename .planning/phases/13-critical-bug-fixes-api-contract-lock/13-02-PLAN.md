---
phase: 13-critical-bug-fixes-api-contract-lock
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/HomePage.tsx
  - frontend/src/pages/RunningPage.tsx
  - frontend/src/lib/contracts.ts
autonomous: true
requirements:
  - BUG-06
  - BUG-07
  - FE-08

must_haves:
  truths:
    - "Both 模式下，evaluateTaskId 在 navigate('/running') 执行前已写入 localStorage"
    - "RunningPage 不再渲染 STEPS 数组文本（假定时动画已移除）"
    - "RunningPage 展示诚实的加载 spinner，API 调用完成后才导航"
    - "frontend/src/lib/contracts.ts 存在并导出 API 类型，npm run build 通过"
  artifacts:
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "Both 模式 await evaluateCampaigns 后再 navigate"
      contains: "await evaluateCampaigns"
    - path: "frontend/src/pages/RunningPage.tsx"
      provides: "移除 STEPS 数组和 setInterval 定时步骤；保留 raceCampaigns() 真实调用"
    - path: "frontend/src/lib/contracts.ts"
      provides: "API contract lock —— 从 api.ts re-export 所有类型，补充 EvaluateSubmitResponse 等缺失类型"
      exports: ["AuthUser", "RacePayload", "RaceResult", "EvaluatePayload", "TaskStatusResponse", "EvaluateResult", "EvaluateSubmitResponse"]
  key_links:
    - from: "frontend/src/pages/HomePage.tsx Both mode handler"
      to: "navigate('/running')"
      via: "await evaluateCampaigns(evalPayload) 在 navigate 之前完成"
      pattern: "await evaluateCampaigns"
    - from: "frontend/src/lib/contracts.ts"
      to: "frontend/src/lib/api.ts"
      via: "import type { ... } from './api' 然后 export type { ... }"
      pattern: "from './api'"
---

<objective>
修复 BUG-06（Both 模式 race condition）、BUG-07（RunningPage 假动画）、实现 FE-08（contracts.ts API 契约锁定）。

Purpose:
- BUG-06：evaluateCampaigns() 未 await 就 navigate，导致 evaluateTaskId 未写入 store，Both 模式结果无法轮询
- BUG-07：RunningPage 用假的 STEPS 定时器误导用户，实际上 Race API 是同步调用，完成即跳转，不需要进度步骤
- FE-08：contracts.ts 不存在 — Phase 14 前端重写将依赖它防止 API drift

Output:
- HomePage.tsx Both 模式：await evaluateCampaigns 后再 navigate
- RunningPage.tsx：移除 STEPS + setInterval 假动画，改为诚实 spinner
- frontend/src/lib/contracts.ts：新建，re-export api.ts 类型，补充缺失类型
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-critical-bug-fixes-api-contract-lock/13-RESEARCH.md

<interfaces>
<!-- 关键接口：executor 直接使用，无需探索代码库 -->

From frontend/src/lib/api.ts (已冻结，本 Plan 不得修改):
```typescript
// Both 模式相关函数
export function evaluateCampaigns(payload: EvaluatePayload):
  Promise<{ task_id: string; set_id: string; campaign_count: number; message: string }>

export function saveEvaluateState(state: { taskId: string; setId: string; payload: EvaluatePayload; result?: EvaluateResult }): void
export function saveBothModeState(state: { evaluateTaskId: string; evaluateSetId: string }): void

// Race 相关函数
export function raceCampaigns(payload: RacePayload): Promise<RaceResult>
export function saveRaceState(state: { payload: RacePayload, result?: RaceResult }): void
export function getRaceState(): { payload: RacePayload, result?: RaceResult } | null

// 已导出类型（contracts.ts 应 re-export 这些）
export type AuthUser, RacePayload, RaceResult, EvaluatePayload, TaskStatusResponse,
  EvaluateResult, UploadImageResponse, VersionInfo, VersionCompareResult
```

From frontend/src/pages/HomePage.tsx (BUG-06 问题代码，约 294-311 行):
```typescript
// Both mode: fire race immediately, evaluate in background
const racePayload = buildRacePayload()
saveRaceState({ payload: racePayload })

const { payload: evalPayload, setId: evalSetId } = buildEvaluatePayload()
setSubmitting(true)

// BUG: evaluateCampaigns 未 await，navigate 立即触发
evaluateCampaigns(evalPayload)
  .then((res) => {
    saveEvaluateState({ taskId: res.task_id, setId: evalSetId, payload: evalPayload })
    saveBothModeState({ evaluateTaskId: res.task_id, evaluateSetId: evalSetId })
  })
  .catch(() => {})

navigate('/running')
```

From frontend/src/pages/RunningPage.tsx (BUG-07 问题代码，完整结构):
```typescript
// 假动画（约 6-36 行）
const STEPS = ['正在初始化...', '正在编译...', ...7 个步骤...]
const [currentStep, setCurrentStep] = useState(0)
const interval = setInterval(() => {
  setCurrentStep(s => (s < STEPS.length - 1 ? s + 1 : s))
}, 2500)
raceCampaigns(state.payload).then(result => {
  saveRaceState({ ...state, result })
  clearInterval(interval)
  navigate('/result')
}).catch(err => {
  clearInterval(interval)
  setError(err.message || '...')
})
// JSX 渲染 STEPS.map(...) 列表
```

Race 路径说明：raceCampaigns() 是同步 HTTP 调用（返回完整 RaceResult），无 task_id，无 TaskManager 轮询。RunningPage 只需等待 Promise resolve/reject。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 BUG-06（Both 模式 await）+ 创建 contracts.ts（FE-08）</name>
  <files>frontend/src/pages/HomePage.tsx, frontend/src/lib/contracts.ts</files>

  <read_first>
    - frontend/src/pages/HomePage.tsx（完整文件：查看 Both 模式 handler 的完整上下文，包括 setSubmitting 调用位置和 buildEvaluatePayload 函数）
    - frontend/src/lib/api.ts（确认所有已导出类型名称，用于 contracts.ts re-export 清单）
  </read_first>

  <action>
    **Step A — 修复 HomePage.tsx BUG-06：**

    找到 Both 模式代码块（包含 "Both mode: fire race immediately" 注释的区域，约 294-311 行）。将以下 fire-and-forget 模式：
    ```typescript
    evaluateCampaigns(evalPayload)
      .then((res) => {
        saveEvaluateState({ taskId: res.task_id, setId: evalSetId, payload: evalPayload })
        saveBothModeState({ evaluateTaskId: res.task_id, evaluateSetId: evalSetId })
      })
      .catch(() => {})

    navigate('/running')
    ```

    替换为 await + try/finally 模式：
    ```typescript
    try {
      const res = await evaluateCampaigns(evalPayload)
      saveEvaluateState({ taskId: res.task_id, setId: evalSetId, payload: evalPayload })
      saveBothModeState({ evaluateTaskId: res.task_id, evaluateSetId: evalSetId })
    } catch {
      // Evaluate POST 失败时静默 — Race 路径仍可用
    } finally {
      setSubmitting(false)
      navigate('/running')
    }
    return
    ```

    注意：
    - Both 模式的整个 handler 函数必须是 async（检查是否已有 async，若无则确保含有此代码块的 handler 是 async function）
    - setSubmitting(false) 从 catch 块移到 finally（保证导航前重置状态）
    - navigate('/running') 移入 finally（保证即使 evaluate 失败也能导航）

    **Step B — 创建 frontend/src/lib/contracts.ts（FE-08）：**

    新建文件，内容如下：
    ```typescript
    // frontend/src/lib/contracts.ts
    // API Contract Lock — frozen 2026-03-18
    //
    // DO NOT change these types without updating the corresponding Flask endpoint.
    // Source of truth: backend/app/api/
    //
    // Rule: Page components MUST import API types from contracts.ts, NOT from api.ts directly.
    // This creates a stable named surface that TypeScript can validate during Phase 14 rewrite.
    //
    // lib/api.ts is FROZEN — do not modify it. Add new types here if needed.

    import type {
      AuthUser,
      RacePayload,
      RaceResult,
      EvaluatePayload,
      TaskStatusResponse,
      EvaluateResult,
      UploadImageResponse,
      VersionInfo,
      VersionCompareResult,
    } from './api'

    // Re-export from api.ts — single source of truth
    export type {
      AuthUser,
      RacePayload,
      RaceResult,
      EvaluatePayload,
      TaskStatusResponse,
      EvaluateResult,
      UploadImageResponse,
      VersionInfo,
      VersionCompareResult,
    }

    // Types not yet in api.ts — endpoint-literal shapes

    /** Response shape from POST /api/campaign/evaluate */
    export type EvaluateSubmitResponse = {
      task_id: string
      set_id: string
      campaign_count: number
      message: string
    }

    /** Single image entry from GET /api/campaign/images/<set_id> */
    export type ImageFileEntry = {
      filename: string
      url: string
    }

    /** Response shape from GET /api/campaign/images/<set_id> */
    export type ListImagesResponse = {
      images: ImageFileEntry[]
    }

    /** Response shape from POST /api/auth/logout */
    export type LogoutResponse = {
      status: string
    }
    ```
  </action>

  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "await evaluateCampaigns" frontend/src/pages/HomePage.tsx` 返回至少一行匹配
    - `grep -n "navigate('/running')" frontend/src/pages/HomePage.tsx` 匹配行不在 await evaluateCampaigns 之前（检查上下文验证顺序）
    - `test -f frontend/src/lib/contracts.ts` 文件存在
    - `grep "from './api'" frontend/src/lib/contracts.ts` 存在 import 行
    - `grep "export type EvaluateSubmitResponse" frontend/src/lib/contracts.ts` 存在
    - `npm run build` 退出码 0（无 TypeScript 编译错误）
  </acceptance_criteria>

  <done>
    - HomePage.tsx Both 模式改为 await evaluateCampaigns 后再 navigate
    - contracts.ts 文件存在，包含从 api.ts 的 re-export 和 3 个新类型
    - npm run build 通过（TypeScript strict 编译成功）
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复 BUG-07（RunningPage 移除假动画）</name>
  <files>frontend/src/pages/RunningPage.tsx</files>

  <read_first>
    - frontend/src/pages/RunningPage.tsx（完整文件：查看 STEPS 定义、setInterval、JSX 渲染部分——替换时必须保留 error 状态、raceCampaigns() 调用、navigate('/result') 逻辑）
  </read_first>

  <action>
    重写 RunningPage.tsx，移除假步骤动画，改为诚实的 spinner。保留：
    - getRaceState() 读取 + 无 payload 时 navigate('/') 的保护逻辑
    - startedRef.current StrictMode 防重复触发
    - raceCampaigns(state.payload).then(...).catch(...) 真实 API 调用
    - error 状态和错误展示 UI

    移除：
    - STEPS 常量数组（整个 7 项数组）
    - currentStep state
    - setInterval 步骤定时器
    - STEPS.map(...) 的 JSX 渲染

    新增：
    - loading state（boolean，初始 true）
    - 简单 spinner 展示（复用现有 Loader2 图标，已导入）

    新的 RunningPage.tsx 完整内容：
    ```typescript
    import { useEffect, useRef, useState } from 'react'
    import { useNavigate } from 'react-router-dom'
    import { getRaceState, raceCampaigns, saveRaceState } from '../lib/api'
    import { Loader2, AlertCircle } from 'lucide-react'

    export function RunningPage() {
      const navigate = useNavigate()
      const [error, setError] = useState<string | null>(null)
      const startedRef = useRef(false)

      useEffect(() => {
        const state = getRaceState()
        if (!state?.payload) {
          navigate('/')
          return
        }

        // Guard against StrictMode double-fire
        if (startedRef.current) return
        startedRef.current = true

        raceCampaigns(state.payload)
          .then(result => {
            saveRaceState({ ...state, result })
            navigate('/result')
          })
          .catch(err => {
            setError(err.message || '推演失败，请检查后端连接')
          })
      }, [navigate])

      if (error) {
        return (
          <div className="min-h-screen bg-primary text-primary-foreground flex flex-col items-center justify-center p-6">
            <div className="w-full max-w-lg space-y-8 text-center">
              <AlertCircle className="h-12 w-12 text-accent mx-auto" />
              <h2 className="font-display text-2xl font-semibold">评估失败</h2>
              <p className="text-primary-foreground/60 text-sm font-mono">{error}</p>
              <button
                onClick={() => navigate('/')}
                className="lab-button lab-button-outline border-primary-foreground/20 text-primary-foreground hover:bg-primary-foreground/10"
              >
                返回构建器
              </button>
            </div>
          </div>
        )
      }

      return (
        <div className="min-h-screen bg-primary text-primary-foreground flex flex-col items-center justify-center p-6">
          <div className="w-full max-w-lg space-y-12 text-center">
            <div className="space-y-4">
              <h2 className="font-display text-4xl font-semibold">评估进行中</h2>
              <p className="text-primary-foreground/60 text-sm font-mono uppercase tracking-wider">
                正在处理 {getRaceState()?.payload?.plans?.length || 0} 个策略方向
              </p>
            </div>

            <div className="flex flex-col items-center gap-6">
              <Loader2 className="h-12 w-12 animate-spin text-primary-foreground/80" />
              <p className="text-primary-foreground/50 text-xs font-mono">
                推演通常需要 10-20 秒，请勿关闭此窗口
              </p>
            </div>
          </div>
        </div>
      )
    }
    ```
  </action>

  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "STEPS" frontend/src/pages/RunningPage.tsx` 返回 0 行（STEPS 常量已移除）
    - `grep -n "setInterval" frontend/src/pages/RunningPage.tsx` 返回 0 行（setInterval 已移除）
    - `grep -n "raceCampaigns" frontend/src/pages/RunningPage.tsx` 返回至少 1 行（真实 API 调用保留）
    - `grep -n "Loader2" frontend/src/pages/RunningPage.tsx` 返回至少 1 行（spinner 存在）
    - `npm run build` 退出码 0
  </acceptance_criteria>

  <done>
    - RunningPage.tsx 不含 STEPS 数组和 setInterval
    - 展示 Loader2 spinner，raceCampaigns() 完成后 navigate('/result')
    - npm run build 通过
  </done>
</task>

</tasks>

<verification>
```bash
# 1. TypeScript 编译（所有前端修改的终极验证）
cd /Users/slr/MiroFishmoody && npm run build

# 2. contracts.ts 存在且含关键类型
grep -n "EvaluateSubmitResponse\|export type AuthUser\|from './api'" \
  /Users/slr/MiroFishmoody/frontend/src/lib/contracts.ts

# 3. BUG-06 修复验证
grep -n "await evaluateCampaigns" /Users/slr/MiroFishmoody/frontend/src/pages/HomePage.tsx

# 4. BUG-07 修复验证（STEPS 和 setInterval 已移除）
grep -c "STEPS\|setInterval" /Users/slr/MiroFishmoody/frontend/src/pages/RunningPage.tsx
# 期望输出: 0

# 5. api.ts 未被修改（契约守护）
git -C /Users/slr/MiroFishmoody diff frontend/src/lib/api.ts
# 期望输出: 空（无改动）
```
</verification>

<success_criteria>
1. npm run build 通过（TypeScript strict 无错误）
2. frontend/src/lib/contracts.ts 存在，re-export api.ts 类型，包含 EvaluateSubmitResponse
3. HomePage.tsx Both 模式包含 await evaluateCampaigns（不是 .then()）
4. RunningPage.tsx 不含 STEPS 常量和 setInterval
5. frontend/src/lib/api.ts 未被修改（git diff 为空）
</success_criteria>

<output>
完成后创建 `.planning/phases/13-critical-bug-fixes-api-contract-lock/13-02-SUMMARY.md`
</output>
