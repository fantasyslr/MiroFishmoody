---
phase: 03-category-persona-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config/personas/moodyplus.json
  - backend/app/config/personas/colored_lenses.json
  - backend/app/services/persona_registry.py
  - backend/tests/test_persona_registry.py
autonomous: true
requirements: [PERS-02]

must_haves:
  truths:
    - "PersonaRegistry.get_personas(category='moodyplus') returns transparent-lens personas"
    - "PersonaRegistry.get_personas(category='colored_lenses') returns colored-lens personas"
    - "PersonaRegistry.get_personas() without category returns default.json (backward compatible)"
    - "Invalid category raises clear error"
  artifacts:
    - path: "backend/app/config/personas/moodyplus.json"
      provides: "Transparent lens persona presets"
      contains: "daily_wearer"
    - path: "backend/app/config/personas/colored_lenses.json"
      provides: "Colored lens persona presets"
      contains: "beauty_first"
    - path: "backend/app/services/persona_registry.py"
      provides: "Category-aware persona loading"
      contains: "def get_personas"
  key_links:
    - from: "backend/app/services/persona_registry.py"
      to: "backend/app/config/personas/moodyplus.json"
      via: "category='moodyplus' loads moodyplus.json"
      pattern: "moodyplus\\.json"
    - from: "backend/app/services/persona_registry.py"
      to: "backend/app/config/personas/colored_lenses.json"
      via: "category='colored_lenses' loads colored_lenses.json"
      pattern: "colored_lenses\\.json"
---

<objective>
Create category-specific persona JSON presets and extend PersonaRegistry to load personas by category.

Purpose: moodyPlus (transparent lenses) and colored_lenses (colored contact lenses) have fundamentally different buyer profiles. Using the same 5 generic personas for both produces inaccurate evaluations.

Output: Two new JSON preset files + PersonaRegistry with `get_personas(category=None)` method.
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-category-persona-config/03-CONTEXT.md

<interfaces>
<!-- Existing PersonaRegistry API that will be extended -->

From backend/app/services/persona_registry.py:
```python
class PersonaRegistry:
    REQUIRED_FIELDS = ["id", "name", "description", "evaluation_focus"]

    def __init__(self, preset_path: str = None):
        # Loads from default.json if no path given

    def get_personas(self) -> List[Dict[str, Any]]:
        """Return a copy of all loaded personas."""

    def get_persona(self, persona_id: str) -> Dict[str, Any]:
        """Return a single persona by id."""
```

From backend/app/config/personas/default.json:
```json
[
    {"id": "daily_wearer", "name": "...", "description": "...", "evaluation_focus": "..."},
    {"id": "acuvue_switcher", ...},
    {"id": "beauty_first", ...},
    {"id": "price_conscious", ...},
    {"id": "eye_health", ...}
]
```

From backend/app/services/audience_panel.py:
```python
class AudiencePanel:
    def __init__(self, llm_client=None, persona_registry=None):
        self._registry = persona_registry or PersonaRegistry()
        self.personas = self._registry.get_personas()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create category persona presets and extend PersonaRegistry</name>
  <files>
    backend/app/config/personas/moodyplus.json,
    backend/app/config/personas/colored_lenses.json,
    backend/app/services/persona_registry.py,
    backend/tests/test_persona_registry.py
  </files>
  <read_first>
    backend/app/services/persona_registry.py,
    backend/app/config/personas/default.json,
    backend/app/services/audience_panel.py
  </read_first>
  <behavior>
    - Test: get_personas(category="moodyplus") returns personas from moodyplus.json
    - Test: get_personas(category="colored_lenses") returns personas from colored_lenses.json
    - Test: get_personas() without category returns default.json personas (backward compat)
    - Test: get_personas(category="nonexistent") raises ValueError with clear message
    - Test: moodyplus.json contains daily_wearer, acuvue_switcher, eye_health + 3 new personas (office_comfort, active_lifestyle, sensitive_eyes)
    - Test: colored_lenses.json contains beauty_first, price_conscious + 3 new personas (makeup_influencer, cosplay_occasion, natural_enhancer)
    - Test: all personas in both files pass schema validation (id, name, description, evaluation_focus)
  </behavior>
  <action>
    1. Create `backend/tests/test_persona_registry.py` with tests for all behaviors above. Run tests — they must FAIL (RED).

    2. Create `backend/app/config/personas/moodyplus.json`:
       - Copy daily_wearer, acuvue_switcher, eye_health from default.json (these are function/health focused)
       - Add 3 new personas:
         - `office_comfort`: 26岁白领，每天电脑前8小时+，最关心长时间佩戴舒适度、防干涩、蓝光。evaluation_focus: 长时间佩戴舒适度声称是否可信、办公场景痛点是否被理解
         - `active_lifestyle`: 24岁运动爱好者，跑步游泳瑜伽，需要镜片不移位不脱落。evaluation_focus: 运动场景适配性、镜片稳定性描述是否具体
         - `sensitive_eyes`: 29岁敏感眼用户，戴镜片容易发红流泪，只信任医疗级材质。evaluation_focus: 敏感眼适配证据、材质安全性是否有临床数据支持

    3. Create `backend/app/config/personas/colored_lenses.json`:
       - Copy beauty_first, price_conscious from default.json (these are aesthetics/value focused)
       - Add 3 new personas:
         - `makeup_influencer`: 23岁美妆博主，美瞳是妆容必备，关注花色独特性、与不同妆容搭配效果。evaluation_focus: 花色差异化是否足够、社交媒体出片效果
         - `cosplay_occasion`: 21岁Coser/特殊场合用户，关注夸张花色和大直径，佩戴频率低但单次效果要求极高。evaluation_focus: 特殊场景视觉冲击力、花色创意度
         - `natural_enhancer`: 27岁职场女性，想要自然放大眼睛但不想被看出戴了美瞳。evaluation_focus: 自然度是否足够、素颜和淡妆场景适配度

    4. Refactor PersonaRegistry:
       - Change `__init__` to accept `config_dir: str = None` instead of `preset_path`
       - `config_dir` defaults to `backend/app/config/personas/`
       - Keep `_preset_path` for backward compatibility: if `preset_path` is passed (keyword), use old behavior
       - Add category-to-filename mapping: `CATEGORY_FILES = {"moodyplus": "moodyplus.json", "colored_lenses": "colored_lenses.json"}`
       - Change `get_personas(self, category: str = None) -> List[Dict[str, Any]]`:
         - If category is None: load from default.json (current behavior)
         - If category in CATEGORY_FILES: load from corresponding file
         - If category not recognized: raise `ValueError(f"Unknown category: {category}. Valid: moodyplus, colored_lenses")`
       - Keep `get_persona(persona_id)` working on the default loaded set
       - Use `_load_file(path)` internal method to DRY up JSON loading + validation

    5. Run tests — they must PASS (GREEN).
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_persona_registry.py -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/config/personas/moodyplus.json exists with 6 personas (grep: "office_comfort")
    - backend/app/config/personas/colored_lenses.json exists with 5 personas (grep: "makeup_influencer")
    - PersonaRegistry.get_personas(category="moodyplus") loads moodyplus.json (grep: "def get_personas.*category")
    - PersonaRegistry.get_personas() still loads default.json (backward compatible)
    - All tests pass
  </acceptance_criteria>
  <done>
    moodyplus.json has 6 personas (3 existing + 3 new), colored_lenses.json has 5 personas (2 existing + 3 new), PersonaRegistry supports category-based loading with backward compatibility, all tests green.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_persona_registry.py -v
```
- get_personas() returns 5 default personas
- get_personas(category="moodyplus") returns 6 personas
- get_personas(category="colored_lenses") returns 5 personas
- Invalid category raises ValueError
</verification>

<success_criteria>
- PersonaRegistry supports category-based persona loading
- Two new JSON preset files exist with domain-appropriate personas
- Backward compatibility preserved (no category = default.json)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-category-persona-config/03-01-SUMMARY.md`
</output>
