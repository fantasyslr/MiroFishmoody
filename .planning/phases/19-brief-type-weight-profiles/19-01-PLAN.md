---
phase: 19-brief-type-weight-profiles
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/campaign.py
  - backend/app/services/brief_weights.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-03
must_haves:
  truths:
    - "BriefType enum 可在 backend 任意模块 import 并用于字段类型注解"
    - "brief_weights.py 输出 3 套权重，每套 6 个维度，各维度权重之和为 1.0"
    - "版本字符串 brand-v1 / seeding-v1 / conversion-v1 可从配置读取"
  artifacts:
    - path: "backend/app/services/brief_weights.py"
      provides: "BRIEF_DIMENSION_WEIGHTS dict + WEIGHT_PROFILE_VERSIONS dict"
      exports: ["BRIEF_DIMENSION_WEIGHTS", "WEIGHT_PROFILE_VERSIONS", "BRIEF_TYPE_VALUES"]
    - path: "backend/app/models/campaign.py"
      provides: "BriefType enum 新增到现有 ProductLine 旁"
      contains: "class BriefType"
  key_links:
    - from: "backend/app/models/campaign.py"
      to: "backend/app/services/brief_weights.py"
      via: "BriefType.value 作为 BRIEF_DIMENSION_WEIGHTS 的 key"
      pattern: "BriefType\\.BRAND\\.value|\"brand\""
---

<objective>
建立 brief_type 数据合约：在 Campaign 模型层新增 BriefType enum，在独立配置文件 brief_weights.py 中声明 3 套维度权重。

Purpose: 下游的 CampaignScorer 和 API 校验层都依赖这两个文件，必须先于所有集成工作存在。
Output: BriefType enum（可 import）+ brief_weights.py（权重配置，可审计，可调参）。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-brief-type-weight-profiles/19-CONTEXT.md

<interfaces>
<!-- 现有 campaign.py 接口，executor 直接复用 -->
From backend/app/models/campaign.py:
```python
class ProductLine(str, Enum):
    COLORED = "colored_lenses"
    MOODYPLUS = "moodyplus"

@dataclass
class Campaign:
    id: str
    name: str
    product_line: ProductLine
    # ... 其余字段
```

From backend/app/models/scoreboard.py:
```python
DIMENSION_KEYS = [
    "thumb_stop",
    "clarity",
    "trust",
    "conversion_readiness",
    "claim_risk",
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 BriefType enum 到 campaign.py</name>
  <files>backend/app/models/campaign.py</files>
  <read_first>backend/app/models/campaign.py</read_first>
  <behavior>
    - BriefType("brand") == BriefType.BRAND
    - BriefType("seeding") == BriefType.SEEDING
    - BriefType("conversion") == BriefType.CONVERSION
    - BriefType("unknown") raises ValueError
    - BriefType 是 str, Enum 子类（与 ProductLine 同模式）
  </behavior>
  <action>
在 ProductLine enum 定义之后、Campaign dataclass 定义之前，新增：

```python
class BriefType(str, Enum):
    """Brief 类型 — 决定评审维度权重"""
    BRAND = "brand"           # 品牌传播
    SEEDING = "seeding"       # 达人种草
    CONVERSION = "conversion" # 转化拉新
```

不修改 Campaign dataclass（brief_type 字段由 API 层在 Plan 19-03 传入 orchestrator，不存入 Campaign 对象）。
不修改文件中任何已有内容。
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run python -c "from app.models.campaign import BriefType; assert BriefType('brand') == BriefType.BRAND; assert BriefType('seeding') == BriefType.SEEDING; assert BriefType('conversion') == BriefType.CONVERSION; print('BriefType OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class BriefType" backend/app/models/campaign.py` 返回 1 行
    - `grep -n "class ProductLine" backend/app/models/campaign.py` 仍返回原有行（未删除）
    - python import 无报错，BriefType.BRAND.value == "brand"
  </acceptance_criteria>
  <done>BriefType enum 可从 backend 任意模块 import，值为 "brand" / "seeding" / "conversion"</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 brief_weights.py 权重配置文件</name>
  <files>backend/app/services/brief_weights.py</files>
  <read_first>backend/app/models/scoreboard.py</read_first>
  <behavior>
    - BRIEF_DIMENSION_WEIGHTS["brand"] 是 dict，keys 与 DIMENSION_KEYS 完全一致
    - BRIEF_DIMENSION_WEIGHTS["brand"] 各 value 之和 == 1.0（允许浮点误差 1e-9）
    - BRIEF_DIMENSION_WEIGHTS["seeding"] 各 value 之和 == 1.0
    - BRIEF_DIMENSION_WEIGHTS["conversion"] 各 value 之和 == 1.0
    - WEIGHT_PROFILE_VERSIONS["brand"] == "brand-v1"
    - WEIGHT_PROFILE_VERSIONS["seeding"] == "seeding-v1"
    - WEIGHT_PROFILE_VERSIONS["conversion"] == "conversion-v1"
    - BRIEF_TYPE_VALUES 是 frozenset，包含 "brand", "seeding", "conversion"
  </behavior>
  <action>
新建文件 `backend/app/services/brief_weights.py`，内容如下（严格按规格，不新增依赖）：

```python
"""
Brief-Type 维度权重配置

每套权重对应一种 brief 类型，6 个维度权重之和必须为 1.0。
当前版本由专家设计，Phase 20 benchmark 命中率测量后可调参。

维度定义（来自 scoreboard.py）：
  thumb_stop          — 停留吸引力
  clarity             — 信息清晰度
  trust               — 信任感
  conversion_readiness — 转化就绪度
  claim_risk          — 声称风险（高 = 差）
  emotional_resonance — 情感共鸣（新增维度，当前 DimensionEvaluator 未输出时权重为 0）
"""

# 每套权重的版本标识符，写入 EvaluationResult.weight_profile_version
WEIGHT_PROFILE_VERSIONS: dict[str, str] = {
    "brand":      "brand-v1",
    "seeding":    "seeding-v1",
    "conversion": "conversion-v1",
}

# 各 brief 类型的维度权重，key 与 DIMENSION_KEYS 对齐
# 注：emotional_resonance 目前 DimensionEvaluator 不输出，权重占位 0.0 不影响评分
BRIEF_DIMENSION_WEIGHTS: dict[str, dict[str, float]] = {
    "brand": {
        "thumb_stop":           0.20,
        "clarity":              0.10,
        "trust":                0.15,
        "conversion_readiness": 0.10,
        "claim_risk":           0.15,
        "emotional_resonance":  0.30,  # 品牌传播核心维度（CONTEXT.md 锁定 0.30）
    },
    "seeding": {
        "thumb_stop":           0.20,
        "clarity":              0.15,
        "trust":                0.10,
        "conversion_readiness": 0.15,
        "claim_risk":           0.10,
        "emotional_resonance":  0.30,  # 达人种草 authenticity 映射为 emotional_resonance
    },
    "conversion": {
        "thumb_stop":           0.15,
        "clarity":              0.15,
        "trust":                0.10,
        "conversion_readiness": 0.35,  # 转化拉新核心维度（CONTEXT.md 锁定 0.35）
        "claim_risk":           0.25,
        "emotional_resonance":  0.00,
    },
}

# 合法值集合，用于 API 层快速校验
BRIEF_TYPE_VALUES: frozenset[str] = frozenset(BRIEF_DIMENSION_WEIGHTS.keys())
```

注意：emotional_resonance 是预留维度（权重占位），DimensionEvaluator 当前不输出该维度时权重自动贡献 0，不影响已有评分维度的归一化。
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run python -c "
from app.services.brief_weights import BRIEF_DIMENSION_WEIGHTS, WEIGHT_PROFILE_VERSIONS, BRIEF_TYPE_VALUES
for bt, w in BRIEF_DIMENSION_WEIGHTS.items():
    total = sum(w.values())
    assert abs(total - 1.0) < 1e-9, f'{bt} weights sum={total}'
assert WEIGHT_PROFILE_VERSIONS['brand'] == 'brand-v1'
assert 'brand' in BRIEF_TYPE_VALUES
print('brief_weights OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `ls backend/app/services/brief_weights.py` 返回文件存在
    - 每套权重之和 == 1.0（浮点精度内）
    - WEIGHT_PROFILE_VERSIONS 含 3 个 key
    - `grep "emotional_resonance" backend/app/services/brief_weights.py` 返回结果（有注释说明）
  </acceptance_criteria>
  <done>brief_weights.py 存在，3 套权重各维度之和为 1.0，BRIEF_TYPE_VALUES 可用于 API 枚举校验</done>
</task>

</tasks>

<verification>
cd /Users/slr/MiroFishmoody/backend && uv run pytest -x -q 2>&1 | tail -5
</verification>

<success_criteria>
- BriefType enum 可在任意后端模块 import，不破坏现有测试
- brief_weights.py 3 套权重之和各为 1.0
- `uv run pytest` 通过（无新增失败）
</success_criteria>

<output>
完成后创建 .planning/phases/19-brief-type-weight-profiles/19-01-SUMMARY.md
</output>
