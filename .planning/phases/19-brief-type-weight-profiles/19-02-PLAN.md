---
phase: 19-brief-type-weight-profiles
plan: "02"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - backend/app/services/submarket_evaluator.py
  - backend/app/services/campaign_scorer.py
  - backend/app/models/evaluation.py
autonomous: true
requirements:
  - EVAL-04
  - EVAL-05
must_haves:
  truths:
    - "选择 conversion brief 时，conversion_readiness 维度权重（0.35）显著高于 brand brief（0.10）"
    - "每条 EvaluationResult.to_dict() 输出中包含 weight_profile_version 字段"
    - "brief_type=None 时回退到等权重（backward compatible）"
  artifacts:
    - path: "backend/app/services/submarket_evaluator.py"
      provides: "DimensionEvaluator.evaluate() 接受 brief_type 参数并应用权重"
      contains: "brief_type"
    - path: "backend/app/services/campaign_scorer.py"
      provides: "CampaignScorer.__init__() 接受 brief_type，传给 DimensionEvaluator"
      contains: "brief_type"
    - path: "backend/app/models/evaluation.py"
      provides: "EvaluationResult 含 weight_profile_version 字段"
      contains: "weight_profile_version"
  key_links:
    - from: "backend/app/services/campaign_scorer.py"
      to: "backend/app/services/submarket_evaluator.py"
      via: "self.dimension_eval.evaluate(campaigns, panel_scores, brief_type=self.brief_type)"
      pattern: "brief_type"
    - from: "backend/app/services/submarket_evaluator.py"
      to: "backend/app/services/brief_weights.py"
      via: "BRIEF_DIMENSION_WEIGHTS[brief_type.value] if brief_type else equal weights"
      pattern: "BRIEF_DIMENSION_WEIGHTS"
---

<objective>
将 brief_type 权重注入 scoring 管道（DimensionEvaluator → CampaignScorer），同时在 EvaluationResult 中新增 weight_profile_version 字段实现结果可追溯。

Purpose: 这是评审偏差修正的核心。无此 plan，conversion brief 在品牌推演中仍会系统性占优。
Output: CampaignScorer 按 brief_type 加权 dimension scores，结果 JSON 带 weight_profile_version。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-brief-type-weight-profiles/19-CONTEXT.md
@.planning/phases/19-brief-type-weight-profiles/19-01-SUMMARY.md

<interfaces>
<!-- 来自 Plan 19-01 的输出 -->
From backend/app/services/brief_weights.py:
```python
BRIEF_DIMENSION_WEIGHTS: dict[str, dict[str, float]]
# e.g. BRIEF_DIMENSION_WEIGHTS["conversion"]["conversion_readiness"] == 0.35

WEIGHT_PROFILE_VERSIONS: dict[str, str]
# e.g. WEIGHT_PROFILE_VERSIONS["brand"] == "brand-v1"

BRIEF_TYPE_VALUES: frozenset[str]
```

From backend/app/models/campaign.py:
```python
class BriefType(str, Enum):
    BRAND = "brand"
    SEEDING = "seeding"
    CONVERSION = "conversion"
```

<!-- 现有接口，executor 直接使用 -->
From backend/app/services/submarket_evaluator.py — DimensionEvaluator.evaluate():
```python
def evaluate(
    self,
    campaigns: List[Campaign],
    panel_scores: List[PanelScore],
) -> List[DimensionScore]:
    ...
    for dimension_key in DIMENSION_KEYS:
        raw_scores = {}
        for cid in all_ids:
            # ... 计算 raw_scores[cid]
        probs = _softmax_probs(raw_scores, temperature=1.5)
        for cid in all_ids:
            results.append(DimensionScore(
                dimension_key=dimension_key,
                score=probs.get(cid, 0),
                raw_score=raw_scores.get(cid, 0),
                ...
            ))
    return results
```

From backend/app/services/campaign_scorer.py — CampaignScorer:
```python
class CampaignScorer:
    def __init__(
        self,
        judge_weights: Dict[str, float] = None,
        persona_weights: Dict[str, float] = None,
    ):
        self.prob_aggregator = ProbabilityAggregator(...)
        self.dimension_eval = DimensionEvaluator()

    def score(self, campaigns, panel_scores, pairwise_results, bt_scores, agent_scores=None):
        ...
        dimension_results = self.dimension_eval.evaluate(campaigns, panel_scores)
        ...
```

From backend/app/models/evaluation.py — EvaluationResult:
```python
@dataclass
class EvaluationResult:
    set_id: str
    rankings: List[CampaignRanking]
    panel_scores: List[PanelScore]
    pairwise_results: List[PairwiseResult]
    summary: str
    assumptions: List[str]
    confidence_notes: List[str]
    scoreboard: Optional[Dict[str, Any]] = None
    resolution_ready_fields: Optional[Dict[str, str]] = None
    visual_diagnostics: Optional[Dict[str, Any]] = None

    def to_dict(self) -> Dict[str, Any]:
        result = {
            "set_id": self.set_id,
            ...
        }
        if self.scoreboard is not None:
            result["scoreboard"] = self.scoreboard
        ...
        return result
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DimensionEvaluator 应用 brief_type 权重</name>
  <files>backend/app/services/submarket_evaluator.py</files>
  <read_first>backend/app/services/submarket_evaluator.py</read_first>
  <behavior>
    - evaluate(campaigns, panel_scores, brief_type=None) — 签名扩展，默认 None
    - brief_type=None 时：所有维度用等权重（1/维度数），与现有行为完全一致
    - brief_type=BriefType.CONVERSION 时：DimensionScore.score 按 BRIEF_DIMENSION_WEIGHTS["conversion"] 各维度权重加权
    - 权重加权逻辑：先通过 softmax 得到 probs（各 campaign 相对排名），再用 brief_type 对应的该维度权重乘以 probs
    - 权重加权后的 dimension score 输出格式不变（仍是 List[DimensionScore]）
    - brief_type 参数只影响 weight 乘数，不影响 softmax 温度或原始分计算
  </behavior>
  <action>
修改 `backend/app/services/submarket_evaluator.py`：

1. 在文件顶部 import 块新增：
```python
from typing import Optional
from ..models.campaign import BriefType
from .brief_weights import BRIEF_DIMENSION_WEIGHTS
```

2. 修改 `DimensionEvaluator.evaluate()` 签名，新增 `brief_type: Optional[BriefType] = None` 参数：
```python
def evaluate(
    self,
    campaigns: List[Campaign],
    panel_scores: List[PanelScore],
    brief_type: Optional[BriefType] = None,
) -> List[DimensionScore]:
```

3. 在 `evaluate()` 方法体内，`for dimension_key in DIMENSION_KEYS:` 循环前，计算权重字典：
```python
# 按 brief_type 加载维度权重，None → 等权重
if brief_type is not None:
    dim_weights = BRIEF_DIMENSION_WEIGHTS.get(brief_type.value, {})
else:
    dim_weights = {}
```

4. 在循环体内 `probs = _softmax_probs(raw_scores, temperature=1.5)` 之后，DimensionScore 构建之前，应用权重乘数：
```python
# 如有权重配置，按维度权重缩放 softmax 概率
dim_weight = dim_weights.get(dimension_key, None)
if dim_weight is not None:
    # 缩放后不再归一化（各维度独立，weight 仅影响跨维度比较时的贡献）
    probs = {k: v * dim_weight for k, v in probs.items()}
```

保留所有现有逻辑不变，只在上述 4 个位置修改/新增。
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run python -c "
from app.services.submarket_evaluator import DimensionEvaluator
from app.models.campaign import BriefType, Campaign, ProductLine
from app.models.evaluation import PanelScore

# 构造最小测试数据
c1 = Campaign(id='c1', name='A', product_line=ProductLine.COLORED,
              target_audience='', core_message='', channels=[], creative_direction='')
c2 = Campaign(id='c2', name='B', product_line=ProductLine.COLORED,
              target_audience='', core_message='', channels=[], creative_direction='')
ps = [
    PanelScore(persona_id='p1', persona_name='P1', campaign_id='c1',
               score=7.0, objections=[], strengths=[], reasoning=''),
    PanelScore(persona_id='p1', persona_name='P1', campaign_id='c2',
               score=5.0, objections=[], strengths=[], reasoning=''),
]

ev = DimensionEvaluator()
# brief_type=None 应该不报错
r_none = ev.evaluate([c1, c2], ps, brief_type=None)
# brief_type=conversion 应该不报错
r_conv = ev.evaluate([c1, c2], ps, brief_type=BriefType.CONVERSION)
assert len(r_none) == len(r_conv), 'length mismatch'
print('DimensionEvaluator brief_type OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "brief_type" backend/app/services/submarket_evaluator.py` 返回 4+ 行
    - `grep "BRIEF_DIMENSION_WEIGHTS" backend/app/services/submarket_evaluator.py` 有 import 和使用
    - 现有 pytest 测试通过：`uv run pytest -x -q`
  </acceptance_criteria>
  <done>DimensionEvaluator.evaluate() 接受 brief_type，brief_type=None 行为与修改前完全一致</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CampaignScorer 接受并传递 brief_type</name>
  <files>backend/app/services/campaign_scorer.py</files>
  <read_first>backend/app/services/campaign_scorer.py</read_first>
  <behavior>
    - CampaignScorer(brief_type=BriefType.CONVERSION) 实例化不报错
    - CampaignScorer(brief_type=None) — 默认值，等同于原有行为
    - scorer.score() 内部调用 self.dimension_eval.evaluate(campaigns, panel_scores, brief_type=self.brief_type)
    - 不修改 score() 的外部调用签名（EvaluationOrchestrator 在 Plan 19-03 更新）
  </behavior>
  <action>
修改 `backend/app/services/campaign_scorer.py`：

1. 在文件顶部 import 块新增（在现有 import 后）：
```python
from ..models.campaign import BriefType
```

2. 修改 `CampaignScorer.__init__()` 签名，新增 `brief_type: Optional[BriefType] = None`：
```python
def __init__(
    self,
    judge_weights: Dict[str, float] = None,
    persona_weights: Dict[str, float] = None,
    brief_type: Optional['BriefType'] = None,
):
    self.prob_aggregator = ProbabilityAggregator(
        judge_weights=judge_weights,
        persona_weights=persona_weights,
    )
    self.dimension_eval = DimensionEvaluator()
    self.brief_type = brief_type
```

3. 在 `score()` 方法中，找到 `dimension_results = self.dimension_eval.evaluate(campaigns, panel_scores)` 这一行，改为：
```python
dimension_results = self.dimension_eval.evaluate(
    campaigns, panel_scores, brief_type=self.brief_type
)
```

只修改上述 3 处，保留所有其他代码不变。
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run python -c "
from app.services.campaign_scorer import CampaignScorer
from app.models.campaign import BriefType
s = CampaignScorer(brief_type=BriefType.BRAND)
assert s.brief_type == BriefType.BRAND
s2 = CampaignScorer()
assert s2.brief_type is None
print('CampaignScorer brief_type OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "brief_type" backend/app/services/campaign_scorer.py` 返回 3+ 行
    - `grep "self.brief_type" backend/app/services/campaign_scorer.py` 在 __init__ 和 evaluate 调用处均出现
    - `uv run pytest -x -q` 通过
  </acceptance_criteria>
  <done>CampaignScorer 接受 brief_type 参数，评分时传给 DimensionEvaluator</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EvaluationResult 新增 weight_profile_version 字段</name>
  <files>backend/app/models/evaluation.py</files>
  <read_first>backend/app/models/evaluation.py</read_first>
  <behavior>
    - EvaluationResult(set_id=..., ..., weight_profile_version="brand-v1") 实例化正常
    - EvaluationResult(set_id=..., ...) 实例化正常（weight_profile_version 默认 None）
    - result.to_dict() 当 weight_profile_version 不为 None 时包含该 key
    - result.to_dict() 当 weight_profile_version 为 None 时不包含该 key（与现有 Optional 字段处理模式一致）
  </behavior>
  <action>
修改 `backend/app/models/evaluation.py`：

1. 在 `EvaluationResult` dataclass 的字段列表末尾（visual_diagnostics 之后）新增：
```python
weight_profile_version: Optional[str] = None  # e.g. "brand-v1", "seeding-v1", "conversion-v1"
```

2. 在 `to_dict()` 方法中，找到 `if self.visual_diagnostics is not None:` 块之后，新增：
```python
if self.weight_profile_version is not None:
    result["weight_profile_version"] = self.weight_profile_version
```

只修改上述 2 处，不删除或修改任何已有字段和 to_dict 逻辑。
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run python -c "
from app.models.evaluation import EvaluationResult
r = EvaluationResult(
    set_id='test', rankings=[], panel_scores=[], pairwise_results=[],
    summary='', assumptions=[], confidence_notes=[],
    weight_profile_version='brand-v1'
)
d = r.to_dict()
assert d.get('weight_profile_version') == 'brand-v1', f'got {d.get(\"weight_profile_version\")}'
r2 = EvaluationResult(
    set_id='test2', rankings=[], panel_scores=[], pairwise_results=[],
    summary='', assumptions=[], confidence_notes=[]
)
assert 'weight_profile_version' not in r2.to_dict()
print('EvaluationResult weight_profile_version OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "weight_profile_version" backend/app/models/evaluation.py` 返回 2+ 行（字段定义 + to_dict 输出）
    - 字段默认 None，不破坏现有调用方（backward compatible）
    - `uv run pytest -x -q` 通过
  </acceptance_criteria>
  <done>EvaluationResult 含 weight_profile_version Optional[str] 字段，to_dict() 按条件输出</done>
</task>

</tasks>

<verification>
cd /Users/slr/MiroFishmoody/backend && uv run pytest -x -q 2>&1 | tail -10
</verification>

<success_criteria>
- DimensionEvaluator 在 conversion brief 下 conversion_readiness 维度权重为 0.35（通过 BRIEF_DIMENSION_WEIGHTS 验证）
- CampaignScorer(brief_type=X).score() 可正常运行
- EvaluationResult.to_dict() 包含 weight_profile_version 字段（当设置时）
- `uv run pytest` 无新增失败
</success_criteria>

<output>
完成后创建 .planning/phases/19-brief-type-weight-profiles/19-02-SUMMARY.md
</output>
