---
phase: 19-brief-type-weight-profiles
plan: "03"
type: execute
wave: 3
depends_on:
  - "19-01"
  - "19-02"
files_modified:
  - backend/app/services/evaluation_orchestrator.py
  - backend/app/api/campaign.py
  - frontend/src/lib/api.ts
  - frontend/src/pages/HomePage.tsx
autonomous: false
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-05
must_haves:
  truths:
    - "POST /api/campaign/evaluate 传入未知 brief_type 字符串返回 HTTP 400，不返回 500"
    - "前端发起评审时 brief_type 字段为必选，未选择时表单提交被阻止并显示中文提示"
    - "评审结果 JSON 包含 weight_profile_version 字段（如 brand-v1）"
    - "不传 brief_type 的旧客户端请求仍返回 202（backward compatible，默认 seeding）"
  artifacts:
    - path: "backend/app/services/evaluation_orchestrator.py"
      provides: "run() 接受 brief_type 参数，传给 CampaignScorer，写入 EvaluationResult"
      contains: "brief_type"
    - path: "backend/app/api/campaign.py"
      provides: "evaluate() 从请求体提取 brief_type，枚举校验，400 on unknown"
      contains: "brief_type"
    - path: "frontend/src/lib/api.ts"
      provides: "EvaluatePayload 包含 brief_type 可选字段"
      contains: "brief_type"
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "radio 选择器 3 选 1，无默认值，未选中时阻止提交"
      contains: "brief_type"
  key_links:
    - from: "frontend/src/pages/HomePage.tsx"
      to: "/api/campaign/evaluate"
      via: "evaluateCampaigns({ ..., brief_type: selectedBriefType })"
      pattern: "brief_type"
    - from: "backend/app/api/campaign.py"
      to: "backend/app/services/evaluation_orchestrator.py"
      via: "orchestrator.run(task_id, campaign_set, category, brief_type)"
      pattern: "brief_type"
    - from: "backend/app/services/evaluation_orchestrator.py"
      to: "backend/app/services/campaign_scorer.py"
      via: "CampaignScorer(brief_type=brief_type_enum)"
      pattern: "CampaignScorer.*brief_type"
---

<objective>
接通 brief_type 的全链路：后端 API 校验 + EvaluationOrchestrator 传导 + 前端 radio 选择器。

Purpose: Plan 19-01/02 建立了合约和 scoring 引擎，本 plan 让用户行为真正触发正确的权重配置。
Output: 前端表单有 3 选 1 必填字段；后端对未知 brief_type 返回 400；结果 JSON 包含 weight_profile_version。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-brief-type-weight-profiles/19-CONTEXT.md
@.planning/phases/19-brief-type-weight-profiles/19-01-SUMMARY.md
@.planning/phases/19-brief-type-weight-profiles/19-02-SUMMARY.md

<interfaces>
<!-- Plan 19-01/02 已建立的接口 -->
From backend/app/models/campaign.py:
```python
class BriefType(str, Enum):
    BRAND = "brand"
    SEEDING = "seeding"
    CONVERSION = "conversion"
```

From backend/app/services/brief_weights.py:
```python
BRIEF_TYPE_VALUES: frozenset[str]  # {"brand", "seeding", "conversion"}
WEIGHT_PROFILE_VERSIONS: dict[str, str]  # {"brand": "brand-v1", ...}
```

From backend/app/services/campaign_scorer.py:
```python
class CampaignScorer:
    def __init__(
        self,
        judge_weights=None,
        persona_weights=None,
        brief_type: Optional[BriefType] = None,
    ): ...
```

From backend/app/models/evaluation.py:
```python
@dataclass
class EvaluationResult:
    # ... 现有字段 ...
    weight_profile_version: Optional[str] = None
```

<!-- 现有 orchestrator 签名 -->
From backend/app/services/evaluation_orchestrator.py:
```python
def run(self, task_id: str, campaign_set, category: str = None) -> dict:
    ...
    scorer = CampaignScorer(
        judge_weights=judge_weights,
        persona_weights=persona_weights,
    )
    ...
    result = EvaluationResult(
        set_id=campaign_set.set_id,
        rankings=rankings,
        ...
    )
```

<!-- 现有 API 调用 orchestrator 处 -->
From backend/app/api/campaign.py:
```python
thread = threading.Thread(
    target=orchestrator.run,
    args=(task_id, campaign_set, category),
    daemon=True,
)
```

<!-- 前端 EvaluatePayload 现有类型 -->
From frontend/src/lib/api.ts:
```typescript
export type EvaluatePayload = {
  set_id: string
  campaigns: Array<{
    campaign_id: string
    name: string
    description?: string
    image_paths?: string[]
  }>
  category?: string
  parent_set_id?: string
}
```

From frontend/src/pages/HomePage.tsx (state hooks):
```typescript
const [productLine, setProductLine] = useState(savedForm?.productLine ?? 'moodyplus')
// 品类选择器在 productLine 之后渲染，brief_type 选择器放在其下方
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EvaluationOrchestrator.run() 接收并传导 brief_type</name>
  <files>backend/app/services/evaluation_orchestrator.py</files>
  <read_first>backend/app/services/evaluation_orchestrator.py</read_first>
  <behavior>
    - run(task_id, campaign_set, category=None, brief_type=None) — 新增 brief_type 参数
    - brief_type=None 时行为与现有完全一致（向后兼容）
    - brief_type=BriefType.BRAND 时，CampaignScorer 以 brief_type=BriefType.BRAND 实例化
    - 保存结果时，EvaluationResult.weight_profile_version 设为 WEIGHT_PROFILE_VERSIONS[brief_type.value]
    - brief_type=None 时 weight_profile_version 设为 None（不写入结果 JSON）
  </behavior>
  <action>
修改 `backend/app/services/evaluation_orchestrator.py`：

1. 新增 import（在现有 import 块末尾）：
```python
from ..models.campaign import BriefType
from .brief_weights import WEIGHT_PROFILE_VERSIONS
```

2. 修改 `run()` 签名，新增 `brief_type: BriefType = None`：
```python
def run(self, task_id: str, campaign_set, category: str = None, brief_type: BriefType = None) -> dict:
```

3. 找到 `scorer = CampaignScorer(judge_weights=judge_weights, persona_weights=persona_weights)` 行，改为：
```python
scorer = CampaignScorer(
    judge_weights=judge_weights,
    persona_weights=persona_weights,
    brief_type=brief_type,
)
```

4. 找到 `EvaluationResult(` 构造调用，在已有字段后新增：
```python
weight_profile_version=WEIGHT_PROFILE_VERSIONS.get(brief_type.value) if brief_type else None,
```

只修改上述 4 处，不删改其他逻辑。
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run python -c "
import inspect
from app.services.evaluation_orchestrator import EvaluationOrchestrator
sig = inspect.signature(EvaluationOrchestrator.run)
assert 'brief_type' in sig.parameters, 'brief_type not in run() signature'
print('EvaluationOrchestrator.run() brief_type param OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "brief_type" backend/app/services/evaluation_orchestrator.py` 返回 4+ 行
    - `grep "WEIGHT_PROFILE_VERSIONS" backend/app/services/evaluation_orchestrator.py` 有 import 和使用
    - `uv run pytest -x -q` 通过
  </acceptance_criteria>
  <done>run() 接受 brief_type，传给 scorer，写入 EvaluationResult.weight_profile_version</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API 层提取 brief_type + 400 枚举校验</name>
  <files>backend/app/api/campaign.py</files>
  <read_first>backend/app/api/campaign.py</read_first>
  <behavior>
    - POST /evaluate 传入 brief_type="brand" → 202 正常发起
    - POST /evaluate 传入 brief_type="unknown_xyz" → 400，响应体 {"error": "brief_type 必须是 brand / seeding / conversion，收到 'unknown_xyz'"}
    - POST /evaluate 不传 brief_type → 202，brief_type=None 传给 orchestrator（向后兼容）
    - brief_type 提取在 category 提取之后、task 创建之前
  </behavior>
  <action>
修改 `backend/app/api/campaign.py`：

1. 新增 import（在现有 from ..models.campaign import ... 行修改，加入 BriefType）：
```python
from ..models.campaign import Campaign, CampaignSet, ProductLine, BriefType
```
同时新增：
```python
from ..services.brief_weights import BRIEF_TYPE_VALUES
```

2. 在 `evaluate()` 函数中，找到 `category = data.get("category")` 这一行，在其后新增：
```python
brief_type_raw = data.get("brief_type")
brief_type_enum = None
if brief_type_raw is not None:
    if brief_type_raw not in BRIEF_TYPE_VALUES:
        return jsonify({
            "error": f"brief_type 必须是 brand / seeding / conversion，收到 '{brief_type_raw}'"
        }), 400
    try:
        brief_type_enum = BriefType(brief_type_raw)
    except ValueError:
        return jsonify({
            "error": f"brief_type 无效值: '{brief_type_raw}'"
        }), 400
```

3. 在 `task_manager.create_task()` 调用的 metadata dict 中，新增：
```python
"brief_type": brief_type_raw,
```

4. 找到 `threading.Thread(target=orchestrator.run, args=(task_id, campaign_set, category), ...)` 这一行，改为：
```python
thread = threading.Thread(
    target=orchestrator.run,
    args=(task_id, campaign_set, category, brief_type_enum),
    daemon=True,
)
```

只修改上述 4 处，不改动其他已有逻辑。
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody/backend && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "brief_type" backend/app/api/campaign.py` 返回 5+ 行
    - `grep "BRIEF_TYPE_VALUES" backend/app/api/campaign.py` 有 import 和条件校验
    - `grep "400" backend/app/api/campaign.py` 的相关行包含 brief_type 错误消息
    - `uv run pytest -x -q` 通过
  </acceptance_criteria>
  <done>unknown brief_type 返回 400；合法值传给 orchestrator；不传 brief_type 向后兼容</done>
</task>

<task type="auto">
  <name>Task 3: 前端 brief_type 选择器 + EvaluatePayload 类型</name>
  <files>frontend/src/lib/api.ts, frontend/src/pages/HomePage.tsx</files>
  <read_first>frontend/src/lib/api.ts, frontend/src/pages/HomePage.tsx</read_first>
  <action>
**frontend/src/lib/api.ts — 修改 EvaluatePayload 类型：**

在 `EvaluatePayload` 类型定义中，新增 `brief_type` 可选字段：
```typescript
export type EvaluatePayload = {
  set_id: string
  campaigns: Array<{
    campaign_id: string
    name: string
    description?: string
    image_paths?: string[]
  }>
  category?: string
  parent_set_id?: string
  brief_type?: 'brand' | 'seeding' | 'conversion'  // 新增
}
```

**frontend/src/pages/HomePage.tsx — 新增 brief_type 状态 + UI：**

1. 在 `const [seasonTag, ...]` state 声明附近新增 state：
```typescript
const [briefType, setBriefType] = useState<'brand' | 'seeding' | 'conversion' | ''>(
  savedForm?.briefType ?? ''
)
```

2. 在现有 `persistForm` 调用中，确保 briefType 被保存（通过在每次调用 persistForm 时传入 briefType）。

3. 在 evaluate / both 模式的表单提交逻辑中（找到 `evaluateCampaigns(` 的构造处），在提交前添加校验：
```typescript
if ((mode === 'evaluate' || mode === 'both') && !briefType) {
  // 显示校验错误，阻止提交
  setUploadError('请选择 Brief 类型（品牌传播 / 达人种草 / 转化拉新）')
  return
}
```

4. 在 `evaluateCampaigns()` 的 payload 对象中新增字段：
```typescript
brief_type: briefType as 'brand' | 'seeding' | 'conversion' | undefined,
```

5. 在页面 JSX 中，找到品类选择器（productLine 相关区域）之后，新增 brief_type radio 组。
   仅在 mode === 'evaluate' || mode === 'both' 时渲染（race 模式不需要 brief 类型）：

```tsx
{(mode === 'evaluate' || mode === 'both') && (
  <div className="space-y-2">
    <label className="block text-sm font-medium text-gray-700">
      Brief 类型 <span className="text-red-500">*</span>
    </label>
    <div className="flex gap-3">
      {[
        { value: 'brand', label: '品牌传播' },
        { value: 'seeding', label: '达人种草' },
        { value: 'conversion', label: '转化拉新' },
      ].map(({ value, label }) => (
        <button
          key={value}
          type="button"
          onClick={() => {
            setBriefType(value as 'brand' | 'seeding' | 'conversion')
            persistForm({ briefType: value })
          }}
          className={`px-4 py-2 rounded-lg border text-sm font-medium transition-colors ${
            briefType === value
              ? 'bg-blue-600 text-white border-blue-600'
              : 'bg-white text-gray-700 border-gray-300 hover:border-blue-400'
          }`}
        >
          {label}
        </button>
      ))}
    </div>
    {(mode === 'evaluate' || mode === 'both') && !briefType && uploadError?.includes('Brief') && (
      <p className="text-sm text-red-500">请选择 Brief 类型</p>
    )}
  </div>
)}
```

6. 当 mode 切换到 'race' 时，不需要清除 briefType（保留已选值，方便用户切换回来）。

**保留所有已有代码不变，仅在上述指定位置新增/修改。**

构建校验：`npm run build` 必须通过。
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "brief_type" frontend/src/lib/api.ts` 返回 1+ 行（EvaluatePayload 类型中）
    - `grep -n "briefType\|brief_type" frontend/src/pages/HomePage.tsx` 返回 5+ 行
    - `grep "品牌传播\|达人种草\|转化拉新" frontend/src/pages/HomePage.tsx` 返回 3 行（radio 选项）
    - `npm run build` 通过，无 TypeScript 错误
  </acceptance_criteria>
  <done>前端表单在 evaluate/both 模式下显示 3 选 1 brief 类型选择器；未选中时提交被阻止并显示中文提示；brief_type 随 payload 发送至后端</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 全链路 brief_type 人工验证</name>
  <what-built>
    全链路 brief_type 接通：
    1. 前端 evaluate 模式表单新增 3 选 1 brief 类型按钮（品牌传播 / 达人种草 / 转化拉新）
    2. 未选择时点击提交显示"请选择 Brief 类型"提示
    3. 后端 POST /api/campaign/evaluate 传入非法 brief_type 返回 400
    4. 合法 brief_type 触发对应权重配置，结果 JSON 包含 weight_profile_version
  </what-built>
  <how-to-verify>
    1. 运行 `npm run dev`，访问 http://localhost:5173
    2. 选择"深度评审"或"联合推演"模式
    3. 确认品类选择器下方出现"Brief 类型 *"标题 + 三个按钮（品牌传播 / 达人种草 / 转化拉新）
    4. 不选择 Brief 类型，点击"开始推演" → 确认出现错误提示，表单未提交
    5. 选择"品牌传播"，正常发起推演 → 推演完成后检查结果 JSON（浏览器 DevTools Network 面板的 /api/campaign/result/{set_id} 响应），确认包含 `"weight_profile_version": "brand-v1"`
    6. （可选）用 curl 测试 400：`curl -X POST http://localhost:5001/api/campaign/evaluate -H "Content-Type: application/json" -d '{"campaigns":[], "brief_type":"invalid_xyz"}' -c /tmp/c.txt` → 应返回 400 + error 消息
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述需要修复的问题</resume-signal>
  <action>暂停，等待人工在浏览器中验证全链路 brief_type 功能后继续</action>
  <verify>人工确认：brief_type 选择器可见，未选中时提交被阻止，结果 JSON 包含 weight_profile_version</verify>
  <done>用户输入 "approved" 或确认所有检查点通过</done>
</task>

</tasks>

<verification>
cd /Users/slr/MiroFishmoody/backend && uv run pytest -x -q 2>&1 | tail -5
cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -5
</verification>

<success_criteria>
- 前端 evaluate/both 模式表单有 brief_type 3 选 1 选择器，未选中阻止提交
- POST /api/campaign/evaluate 传入 brief_type="invalid" 返回 HTTP 400
- 评审结果 JSON 包含 weight_profile_version 字段（如 "brand-v1"）
- `uv run pytest` 和 `npm run build` 均通过
- 人工验证通过
</success_criteria>

<output>
完成后创建 .planning/phases/19-brief-type-weight-profiles/19-03-SUMMARY.md
</output>
