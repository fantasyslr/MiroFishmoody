---
phase: 14-frontend-rewrite-core-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/EvaluateResultPage.tsx
  - frontend/src/pages/ResultPage.tsx
autonomous: true
requirements:
  - FE-02
  - FE-06
must_haves:
  truths:
    - "打开 EvaluateResultPage，无需点任何 tab，顶部即可看到冠军方案名称 + 综合分 + 一句话推荐"
    - "Race winner 与 Evaluate winner 名称不同时，两个结果页（ResultPage 和 EvaluateResultPage）顶部均显示固定横幅，不需要切换 tab"
    - "Race winner 与 Evaluate winner 一致时，横幅不显示"
  artifacts:
    - path: "frontend/src/pages/EvaluateResultPage.tsx"
      provides: "Winner hero card above tab nav + cross-path conflict badge"
      contains: "winner-hero"
    - path: "frontend/src/pages/ResultPage.tsx"
      provides: "Cross-path conflict badge from race side"
      contains: "Race 与 Evaluate 冠军不一致"
  key_links:
    - from: "EvaluateResultPage.tsx rankings[0]"
      to: "WinnerHero component"
      via: "rankings sorted by rank asc, index 0 = champion, rendered before tab nav"
    - from: "ResultPage.tsx getRaceState().result.observed_baseline.ranking[0].plan.name"
      to: "conflict badge"
      via: "compared against getEvaluateState().result.rankings[0].campaign_name"
---

<objective>
EvaluateResultPage winner-first 布局 + 两个结果页跨路径不一致警示 badge。

Purpose: 用户完成 Evaluate 后首屏就能看到冠军，无需先切 tab；Both 模式下 Race/Evaluate 冠军不一致时，两页顶部均有显眼横幅警告。
Output: 修改后的 EvaluateResultPage.tsx 和 ResultPage.tsx，npm run build 通过。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/slr/MiroFishmoody/.planning/PROJECT.md
@/Users/slr/MiroFishmoody/.planning/STATE.md
@/Users/slr/MiroFishmoody/CLAUDE.md

<interfaces>
<!-- Key contracts extracted from codebase. -->

From frontend/src/lib/api.ts (FROZEN):
```typescript
// EvaluateResultPage uses:
export function getEvaluateState(): EvaluateState | null
// EvaluateState.result: EvaluateResult
// EvaluateResult.rankings: EvalRanking[]
// EvalRanking: { campaign_id, campaign_name, rank, composite_score, panel_avg, pairwise_wins, pairwise_losses, verdict, top_strengths, top_objections }
// EvaluateResult.scoreboard: EvalScoreboard | undefined
// EvalScoreboard.campaigns: [{ campaign_id, campaign_name, overall_score, dimension_scores, verdict_rationale }]

// ResultPage uses:
export function getRaceState(): RaceState | null
// RaceState.result.observed_baseline.ranking: RankingEntry[]
// RankingEntry: { rank, plan: { name }, score, ... }

// Both mode cross-comparison:
export function getBothModeState(): BothModeState | null
// BothModeState: { evaluateTaskId, evaluateSetId }
```

Current EvaluateResultPage structure (key sections):
- Header section (h1 + buttons)
- Tab navigation (ranking / persona / pairwise)
- Tab content (RankingTab renders below nav)

Winner hero should be inserted BETWEEN header section and tab navigation, always visible.

Current ResultPage structure:
- Header section (h1 + recommendation + export buttons)
- Both Mode evaluate link section (conditionally rendered)
- Side-by-side comparison section
- Radar chart section
- Rankings list section

Conflict badge on ResultPage should go in header section, after recommendation block, before Both Mode section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EvaluateResultPage — Winner Hero Card</name>
  <files>frontend/src/pages/EvaluateResultPage.tsx</files>
  <read_first>
    - frontend/src/pages/EvaluateResultPage.tsx (完整读取，了解现有 rankings/scoreboard 结构和布局顺序)
    - frontend/src/lib/api.ts (确认 EvalRanking 类型字段)
  </read_first>
  <action>
修改 `frontend/src/pages/EvaluateResultPage.tsx`，在 Tab Navigation 之前插入 Winner Hero Card：

**插入位置：** 在 `{/* Tab Navigation */}` 注释和 `<div className="flex gap-2 border-b border-border pb-0">` 之前。

**插入内容（在 exportRef div 内部、tab nav 之前）：**
```tsx
{/* Winner Hero Card — always visible, no tab switch needed */}
{rankings.length > 0 && (() => {
  const champion = rankings[0]  // already sorted by rank asc
  const sbChampion = scoreboard?.campaigns.find(c => c.campaign_id === champion.campaign_id)
  const verdictStyle = VERDICT_STYLE[champion.verdict] ?? VERDICT_STYLE.revise
  return (
    <div
      className="lab-card p-6 border-2 border-primary/20 bg-primary/5 space-y-3"
      data-testid="winner-hero"
    >
      <div className="flex items-start justify-between gap-4">
        <div className="flex items-center gap-3">
          <div className="flex flex-col items-center justify-center w-12 h-12 rounded-sm bg-primary text-primary-foreground font-display text-2xl font-semibold shrink-0">
            1
          </div>
          <div>
            <div className="flex items-center gap-2 mb-1">
              <h2 className="font-display text-2xl font-semibold text-primary">{champion.campaign_name}</h2>
              <span className={`text-[10px] uppercase tracking-wider font-semibold px-2 py-0.5 rounded-sm border ${verdictStyle.bg} ${verdictStyle.text}`}>
                {verdictStyle.label}
              </span>
            </div>
            <div className="flex items-center gap-3 text-xs text-muted-foreground font-mono">
              <span>综合分 <strong className="text-foreground">{champion.composite_score.toFixed(1)}</strong></span>
              <span>&bull;</span>
              <span>评审均分 {champion.panel_avg.toFixed(1)}</span>
              <span>&bull;</span>
              <span>胜 {champion.pairwise_wins} / 负 {champion.pairwise_losses}</span>
            </div>
          </div>
        </div>
        <div className="text-right shrink-0">
          <div className="text-xs text-muted-foreground uppercase tracking-wider mb-1">综合分</div>
          <div className="font-mono text-3xl font-medium text-primary">{champion.composite_score.toFixed(1)}</div>
        </div>
      </div>
      {sbChampion?.verdict_rationale && (
        <p className="text-sm text-muted-foreground leading-relaxed border-t border-primary/10 pt-3">
          {sbChampion.verdict_rationale}
        </p>
      )}
    </div>
  )
})()}
```

同时在 EvaluateResultPage 中添加跨路径不一致 badge（在 Winner Hero Card 之前，即 exportRef 内部最顶部）：

```tsx
{/* Cross-path conflict badge */}
{(() => {
  try {
    const raceState = getRaceState()
    const evalWinner = rankings[0]?.campaign_name
    const raceWinner = raceState?.result?.observed_baseline?.ranking?.[0]?.plan?.name
    if (!evalWinner || !raceWinner || evalWinner === raceWinner) return null
    return (
      <div className="flex items-center gap-3 px-4 py-3 rounded-sm bg-amber-50 border border-amber-300 text-amber-800">
        <AlertTriangle className="h-4 w-4 shrink-0 text-amber-500" />
        <span className="text-sm font-medium">
          ⚠ Race 与 Evaluate 冠军不一致 — Race: <strong>{raceWinner}</strong> vs Evaluate: <strong>{evalWinner}</strong>
        </span>
      </div>
    )
  } catch {
    return null
  }
})()}
```

需要在 import 中补充 `getRaceState`（从 '../lib/api' 导入）。

AlertTriangle 已在现有 import 中（lucide-react），直接使用。

**保留所有现有逻辑，不删除任何内容。**
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "winner-hero\|Winner Hero" /Users/slr/MiroFishmoody/frontend/src/pages/EvaluateResultPage.tsx` 返回至少 1 行
    - `grep -n "Race 与 Evaluate 冠军不一致" /Users/slr/MiroFishmoody/frontend/src/pages/EvaluateResultPage.tsx` 返回 1 行
    - `grep -n "getRaceState" /Users/slr/MiroFishmoody/frontend/src/pages/EvaluateResultPage.tsx` 返回 import 行和使用行
    - `npm run build` 通过
  </acceptance_criteria>
  <done>
    - Winner Hero Card 在 tab nav 之上渲染，首屏可见
    - 跨路径不一致时显示 amber 横幅（包含 Race 和 Evaluate 冠军名称）
    - 一致时横幅不显示
  </done>
</task>

<task type="auto">
  <name>Task 2: ResultPage — 跨路径不一致 badge</name>
  <files>frontend/src/pages/ResultPage.tsx</files>
  <read_first>
    - frontend/src/pages/ResultPage.tsx (完整读取，了解 both mode 状态读取和 header section 结构)
    - frontend/src/lib/api.ts (确认 getEvaluateState / getEvaluateResult 类型)
  </read_first>
  <action>
修改 `frontend/src/pages/ResultPage.tsx`，在 Header section 内（recommendation 块之后、Both Mode section 之前）添加跨路径冲突 badge。

**前提理解：** ResultPage 在 Both 模式完成后，已通过 `handleEvalNavigate` 获取了 evaluate 结果并保存到 evaluateState（但这发生在用户点击"查看深度评审结果"之后）。Race 结果页渲染时，evaluate 可能还在跑，所以需要对 evaluate 状态和 evalStatus 做防御判断。

**在现有的 `{/* Recommendation */}` 块之后（仍在 `<section className="space-y-6">` 内），添加：**

```tsx
{/* Cross-path conflict badge — show when Race and Evaluate winners differ */}
{(() => {
  if (!bothMode) return null  // 只在 Both 模式下有意义
  if (evalStatus !== 'completed') return null  // evaluate 未完成时不显示
  try {
    const evalState = getEvaluateState()
    const evalWinner = evalState?.result?.rankings?.[0]?.campaign_name
    const raceWinner = topEntry?.plan?.name
    if (!evalWinner || !raceWinner || evalWinner === raceWinner) return null
    return (
      <div className="flex items-center gap-3 px-4 py-3 rounded-sm bg-amber-50 border border-amber-300 text-amber-800">
        <AlertTriangle className="h-4 w-4 shrink-0 text-amber-500" />
        <span className="text-sm font-medium">
          ⚠ Race 与 Evaluate 冠军不一致 — Race: <strong>{raceWinner}</strong> vs Evaluate: <strong>{evalWinner}</strong>
        </span>
      </div>
    )
  } catch {
    return null
  }
})()}
```

需要在 import 中补充 `getEvaluateState`（检查现有 import，api.ts 已有此函数，ResultPage 顶部 import 已包含 `getEvaluateState`，确认后直接使用，如未包含则补充）。

AlertTriangle 已在现有 lucide-react import 中，直接使用。

**保留所有现有逻辑，不删除任何内容。**
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Race 与 Evaluate 冠军不一致" /Users/slr/MiroFishmoody/frontend/src/pages/ResultPage.tsx` 返回 1 行
    - `grep -n "getEvaluateState" /Users/slr/MiroFishmoody/frontend/src/pages/ResultPage.tsx` 返回至少 1 行
    - `npm run build` 通过，无 TypeScript 错误
  </acceptance_criteria>
  <done>
    - Race 结果页在 Both 模式 + evaluate 已完成 + 冠军不一致时显示 amber 横幅
    - 非 Both 模式不显示
    - evaluate 尚未完成时不显示
  </done>
</task>

</tasks>

<verification>
1. `npm run build` 通过
2. `grep -rn "winner-hero" /Users/slr/MiroFishmoody/frontend/src/pages/` 返回 EvaluateResultPage.tsx 的行
3. `grep -rn "Race 与 Evaluate 冠军不一致" /Users/slr/MiroFishmoody/frontend/src/pages/` 返回两个文件各 1 行
</verification>

<success_criteria>
- EvaluateResultPage 顶部（tab nav 之上）有 winner hero card，首屏可见冠军名称和综合分
- 两个结果页在冠军不一致时均显示 amber 警示横幅
- npm run build 通过
</success_criteria>

<output>
After completion, create `/Users/slr/MiroFishmoody/.planning/phases/14-frontend-rewrite-core-pages/14-02-SUMMARY.md`
</output>
