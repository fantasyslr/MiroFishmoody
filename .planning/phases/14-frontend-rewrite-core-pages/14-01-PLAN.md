---
phase: 14-frontend-rewrite-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/HomePage.tsx
  - frontend/src/lib/homeFormStorage.ts
autonomous: true
requirements:
  - FE-01
  - FE-03
must_haves:
  truths:
    - "在 HomePage 填写部分表单后导航离开再回来，所有字段（mode、market、productLine、sortBy、seasonTag、plans 数组）完整恢复，无弹窗"
    - "选择品类后，右侧侧边栏立即显示对应品类的人格名称列表和数量（moodyPlus 显示 6 个，colored_lenses 显示 5 个）"
    - "提交成功后 sessionStorage 中的表单数据被清除"
  artifacts:
    - path: "frontend/src/lib/homeFormStorage.ts"
      provides: "sessionStorage save/restore helpers for HomePage form"
      exports: ["saveHomeForm", "loadHomeForm", "clearHomeForm", "HOME_FORM_KEY"]
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "Form with sessionStorage persistence and persona preview sidebar"
      contains: "saveHomeForm"
  key_links:
    - from: "HomePage.tsx onChange handlers"
      to: "homeFormStorage.saveHomeForm()"
      via: "every field onChange calls saveHomeForm with current state snapshot"
    - from: "HomePage.tsx useEffect (mount)"
      to: "homeFormStorage.loadHomeForm()"
      via: "useState initializer or useEffect restores state on first render"
    - from: "HomePage.tsx handleSubmit success path"
      to: "homeFormStorage.clearHomeForm()"
      via: "called after navigate('/running') or navigate('/evaluate')"
---

<objective>
HomePage 表单状态 sessionStorage 持久化 + 品类人格预览侧边栏。

Purpose: 用户在 HomePage 填写复杂表单时导航中断（如查看历史结果）后，回来不需要重新填写；选品类时立即看到对应人格评审团列表。
Output: homeFormStorage.ts 工具模块 + HomePage.tsx 修改版，npm run build 通过。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/slr/MiroFishmoody/.planning/PROJECT.md
@/Users/slr/MiroFishmoody/.planning/ROADMAP.md
@/Users/slr/MiroFishmoody/.planning/STATE.md
@/Users/slr/MiroFishmoody/CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From frontend/src/lib/api.ts (FROZEN — do not modify):
```typescript
// Functions used by HomePage — already imported
export function saveRaceState(state: Partial<RaceState>): void
export function saveEvaluateState(state: Partial<EvaluateState>): void
export function evaluateCampaigns(payload: EvaluatePayload): Promise<{ task_id: string }>
export function saveBothModeState(state: BothModeState): void
export function getIterateState(): IterateState | null
export function clearIterateState(): void
export function uploadCampaignImage(file: File, setId: string, planId: string): Promise<{ url: string }>
```

From frontend/src/pages/HomePage.tsx (top-level state to persist):
```typescript
// All these useState values must be saved/restored:
const [mode, setMode] = useState<SimulationMode>('race')          // 'race' | 'evaluate' | 'both'
const [market, setMarket] = useState('cn')
const [productLine, setProductLine] = useState('moodyplus')
const [sortBy, setSortBy] = useState<RacePayload['sort_by']>('roas_mean')
const [seasonTag, setSeasonTag] = useState('')
const [plans, setPlans] = useState<CampaignPlan[]>([...])         // array of plan objects

// Do NOT persist: planImages (blob URLs invalid after reload), submitting, uploadError
// Do NOT persist: draftSetId (regenerate each session), iterateState (comes from api.ts)
```

Persona data (hardcoded in plan — no API call needed):
```typescript
// moodyplus (6 personas):
// '日抛隐形眼镜老用户', 'Acuvue Define 现有用户', '眼健康关注者', '办公室长时间佩戴用户', '运动爱好者', '敏感眼用户'
// colored_lenses (5 personas):
// '美瞳新用户（颜值驱动）', '价格敏感用户', '美妆博主', 'Coser/特殊场合用户', '自然放大需求用户'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 homeFormStorage.ts 工具模块</name>
  <files>frontend/src/lib/homeFormStorage.ts</files>
  <read_first>
    - frontend/src/pages/HomePage.tsx (了解需要持久化的 state 字段类型)
    - frontend/src/lib/api.ts (确认 CampaignPlan、RacePayload 类型)
  </read_first>
  <action>
创建 `frontend/src/lib/homeFormStorage.ts`，实现以下内容：

```typescript
// frontend/src/lib/homeFormStorage.ts
// sessionStorage persistence for HomePage form state

import type { CampaignPlan, RacePayload } from './api'

export const HOME_FORM_KEY = 'miro_home_form_v1'

export type HomeFormSnapshot = {
  mode: 'race' | 'evaluate' | 'both'
  market: string
  productLine: string
  sortBy: RacePayload['sort_by']
  seasonTag: string
  plans: CampaignPlan[]
}

export function saveHomeForm(snapshot: HomeFormSnapshot): void {
  try {
    sessionStorage.setItem(HOME_FORM_KEY, JSON.stringify(snapshot))
  } catch {
    // sessionStorage quota exceeded or unavailable — silently ignore
  }
}

export function loadHomeForm(): HomeFormSnapshot | null {
  try {
    const raw = sessionStorage.getItem(HOME_FORM_KEY)
    if (!raw) return null
    const parsed = JSON.parse(raw) as HomeFormSnapshot
    // Basic validation — if plans array missing or empty, return null
    if (!parsed.plans || parsed.plans.length === 0) return null
    return parsed
  } catch {
    return null
  }
}

export function clearHomeForm(): void {
  try {
    sessionStorage.removeItem(HOME_FORM_KEY)
  } catch {
    // ignore
  }
}
```

注意：
- 不持久化 planImages（blob URL 跨 session 无效）
- 不持久化 draftSetId（每次 session 重新生成）
- plans 数组中的 image_paths 会被保留（服务端 URL 持久有效）
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "saveHomeForm\|loadHomeForm\|clearHomeForm\|HOME_FORM_KEY" /Users/slr/MiroFishmoody/frontend/src/lib/homeFormStorage.ts` 返回 4 行匹配
    - `grep -n "HomeFormSnapshot" /Users/slr/MiroFishmoody/frontend/src/lib/homeFormStorage.ts` 返回类型定义行
    - `npm run build` 通过，无 TypeScript 错误
  </acceptance_criteria>
  <done>homeFormStorage.ts 存在，导出 saveHomeForm / loadHomeForm / clearHomeForm / HOME_FORM_KEY，TypeScript strict 通过</done>
</task>

<task type="auto">
  <name>Task 2: HomePage 接入 sessionStorage 持久化 + 品类人格预览</name>
  <files>frontend/src/pages/HomePage.tsx</files>
  <read_first>
    - frontend/src/pages/HomePage.tsx (完整读取现有实现，了解所有 state 和 onChange)
    - frontend/src/lib/homeFormStorage.ts (Task 1 刚创建，确认导出接口)
  </read_first>
  <action>
修改 `frontend/src/pages/HomePage.tsx`，做以下三处改动：

**改动 1: 在文件顶部 import 中添加：**
```typescript
import { saveHomeForm, loadHomeForm, clearHomeForm } from '../lib/homeFormStorage'
```

**改动 2: 将所有 useState 初始值改为从 sessionStorage 恢复（静默恢复，无弹窗）：**

修改 mode、market、productLine、sortBy、seasonTag、plans 的 useState 初始值，使用 loadHomeForm() 的返回值：

```typescript
const savedForm = loadHomeForm()

const [mode, setMode] = useState<SimulationMode>(savedForm?.mode ?? 'race')
const [market, setMarket] = useState(savedForm?.market ?? 'cn')
const [productLine, setProductLine] = useState(savedForm?.productLine ?? 'moodyplus')
const [sortBy, setSortBy] = useState<RacePayload['sort_by']>(savedForm?.sortBy ?? 'roas_mean')
const [seasonTag, setSeasonTag] = useState(savedForm?.seasonTag ?? '')
```

plans 的 useState 改为：
```typescript
const [plans, setPlans] = useState<CampaignPlan[]>(() => {
  if (savedForm?.plans && savedForm.plans.length > 0) return savedForm.plans
  return [
    makePlan({ name: 'Plan A', theme: 'science_credibility' }),
    makePlan({ name: 'Plan B', theme: 'comfort_beauty', platform: 'instagram' }),
  ]
})
```

注意：savedForm 需要在组件函数体内、useState 调用之前声明，用 `const savedForm = loadHomeForm()` 一次性读取（不用 useRef，不用 useEffect）。

**改动 3: 在所有 onChange 回调中调用 saveHomeForm：**

创建一个 helper 函数（放在 handleSubmit 之前）：
```typescript
const persistForm = (overrides: Partial<Parameters<typeof saveHomeForm>[0]> = {}) => {
  saveHomeForm({
    mode,
    market,
    productLine,
    sortBy,
    seasonTag,
    plans,
    ...overrides,
  })
}
```

然后在每个 setXxx 调用点之后（或替换为包装函数）调用 persistForm。具体：
- setMode 的地方改为：`setMode(v.value); persistForm({ mode: v.value })`
- setMarket 的地方改为：`setMarket(e.target.value); persistForm({ market: e.target.value })`
- setProductLine 的地方改为：`setProductLine(e.target.value); persistForm({ productLine: e.target.value })`
- setSortBy 的地方改为：`setSortBy(v); persistForm({ sortBy: v })`
- setSeasonTag 的地方改为：`setSeasonTag(e.target.value); persistForm({ seasonTag: e.target.value })`
- updatePlan 函数内 setPlans 调用后：增加 `persistForm({ plans: nextPlans })` — 需要将 setPlans 的 functional update 改为先计算 nextPlans 再 setPlans(nextPlans)，然后 persistForm。

**改动 4: 在 handleSubmit 成功路径调用 clearHomeForm：**

在 navigate('/running')、navigate('/evaluate') 调用之前（或 finally 块中）调用 `clearHomeForm()`。

**改动 5: 品类人格预览 — 在右侧 sticky sidebar 中，"评估矩阵" 区域之前，添加人格预览区块：**

在现有的 sidebar（`<div className="sticky top-24 space-y-6">` 内 `<div className="lab-card p-5 space-y-5 bg-card/50">` 之前）插入：

```tsx
{/* 品类人格预览 */}
<div className="lab-card p-5 space-y-3">
  <h3 className="text-xs uppercase tracking-wider font-bold text-primary">
    评审团
    <span className="ml-2 text-muted-foreground font-normal normal-case">
      {productLine === 'moodyplus' ? '透明片' : '彩片'} · {PERSONA_PREVIEW[productLine].length} 位
    </span>
  </h3>
  <ul className="space-y-1.5">
    {PERSONA_PREVIEW[productLine].map((name, i) => (
      <li key={i} className="text-sm text-muted-foreground flex items-center gap-2">
        <span className="w-5 h-5 rounded-full bg-primary/10 text-primary text-[10px] flex items-center justify-center shrink-0 font-mono">
          {i + 1}
        </span>
        {name}
      </li>
    ))}
  </ul>
</div>
```

在文件顶部（组件外部）定义常量：
```typescript
const PERSONA_PREVIEW: Record<string, string[]> = {
  moodyplus: [
    '日抛隐形眼镜老用户',
    'Acuvue Define 现有用户',
    '眼健康关注者',
    '办公室长时间佩戴用户',
    '运动爱好者',
    '敏感眼用户',
  ],
  colored_lenses: [
    '美瞳新用户（颜值驱动）',
    '价格敏感用户',
    '美妆博主',
    'Coser/特殊场合用户',
    '自然放大需求用户',
  ],
}
```

注意：
- lib/api.ts 不可修改（FROZEN）
- 不引入任何新依赖
- 不改变现有上传、提交等逻辑
- 仅在上述 5 处改动
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "loadHomeForm\|saveHomeForm\|clearHomeForm" /Users/slr/MiroFishmoody/frontend/src/pages/HomePage.tsx` 分别至少返回 1 行
    - `grep -n "PERSONA_PREVIEW" /Users/slr/MiroFishmoody/frontend/src/pages/HomePage.tsx` 返回定义行和使用行
    - `grep -n "评审团" /Users/slr/MiroFishmoody/frontend/src/pages/HomePage.tsx` 返回人格预览标题行
    - `npm run build` 通过，无 TypeScript 错误
  </acceptance_criteria>
  <done>
    - onChange 中 saveHomeForm 被调用，state 实时持久化
    - 组件 mount 时从 sessionStorage 静默恢复表单字段
    - 右侧 sidebar 显示品类对应人格列表（moodyPlus 6 个 / colored_lenses 5 个）
    - handleSubmit 成功后 clearHomeForm 被调用
  </done>
</task>

</tasks>

<verification>
1. `npm run build` 在 /Users/slr/MiroFishmoody 下通过，无 TypeScript 错误
2. `grep "saveHomeForm" /Users/slr/MiroFishmoody/frontend/src/pages/HomePage.tsx` 返回多行（每个字段的 onChange 处）
3. `grep "PERSONA_PREVIEW\[productLine\]" /Users/slr/MiroFishmoody/frontend/src/pages/HomePage.tsx` 返回使用行
4. `cat /Users/slr/MiroFishmoody/frontend/src/lib/homeFormStorage.ts` 存在且包含 4 个导出
</verification>

<success_criteria>
- homeFormStorage.ts 创建，导出 saveHomeForm / loadHomeForm / clearHomeForm / HOME_FORM_KEY
- HomePage.tsx 在每个字段 onChange 时调用 saveHomeForm，mount 时静默恢复
- 右侧侧边栏显示品类人格预览，切换品类立即更新，moodyPlus 6 人格，colored_lenses 5 人格
- `npm run build` 通过
</success_criteria>

<output>
After completion, create `/Users/slr/MiroFishmoody/.planning/phases/14-frontend-rewrite-core-pages/14-01-SUMMARY.md`
</output>
