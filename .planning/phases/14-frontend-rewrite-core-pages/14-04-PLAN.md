---
phase: 14-frontend-rewrite-core-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/exportUtils.ts
autonomous: true
requirements:
  - FE-04
must_haves:
  truths:
    - "5 方案满载（5 个 campaign，每个有图片）的 EvaluateResultPage 能成功导出 PDF 和 PNG，不崩溃、不静默失败"
    - "导出内容按页分页（内容超出单页时追加页面），不裁剪内容"
    - "导出 PNG 图片分辨率为 2x（Retina），width 至少 1200px"
  artifacts:
    - path: "frontend/src/lib/exportUtils.ts"
      provides: "Reliable PDF (multi-page) and PNG export for 5-campaign result pages"
      contains: "addPage"
  key_links:
    - from: "captureElementAsPDF"
      to: "jsPDF.addPage()"
      via: "when imgHeight > availableHeight, split canvas into multiple pages"
---

<objective>
导出工具修复：html2canvas 5 方案满载测试 + PDF 多页支持。

Purpose: 现有 exportUtils.ts 在内容超出 A4 单页高度时截断内容。5 方案评审结果页内容量大，必须分页输出。
Output: 修改后的 exportUtils.ts，支持多页 PDF 和可靠的 PNG 导出。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/slr/MiroFishmoody/.planning/PROJECT.md
@/Users/slr/MiroFishmoody/.planning/STATE.md
@/Users/slr/MiroFishmoody/CLAUDE.md

<interfaces>
<!-- Key contracts extracted from codebase. -->

Current exportUtils.ts (problem area):
```typescript
// Current PDF logic has this bug — single page, content gets cut:
const finalHeight = Math.min(imgHeight, availableHeight)  // TRUNCATES!
const finalWidth = imgHeight > availableHeight
  ? (canvas.width / canvas.height) * finalHeight
  : imgWidth
pdf.addImage(imgData, 'PNG', margin, margin + titleHeight, finalWidth, finalHeight)
// Only one pdf.addImage call — no pagination
```

jsPDF API (v4.x — installed):
```typescript
// Multi-page approach:
pdf.addImage(imgData, 'PNG', x, y, width, height)  // can use negative y to offset slice
pdf.addPage()  // add new page
// Page dimensions:
const pageWidth = pdf.internal.pageSize.getWidth()   // 210mm for A4
const pageHeight = pdf.internal.pageSize.getHeight() // 297mm for A4
```

html2canvas options (v1.4.1 — installed):
```typescript
html2canvas(element, {
  scale: 2,              // 2x retina
  backgroundColor: '#FFFFFF',
  useCORS: true,
  logging: false,
  // For large elements:
  windowWidth: element.scrollWidth,
  windowHeight: element.scrollHeight,
})
```

Existing callers (do NOT change their signatures):
- EvaluateResultPage.tsx: captureElementAsPDF(exportRef.current, filename, title)
- EvaluateResultPage.tsx: captureElementAsImage(exportRef.current, filename)
- ResultPage.tsx: captureElementAsPDF(exportRef.current, filename, title)
- ResultPage.tsx: captureElementAsImage(exportRef.current, filename)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: exportUtils.ts — 多页 PDF + 满载稳定性修复</name>
  <files>frontend/src/lib/exportUtils.ts</files>
  <read_first>
    - frontend/src/lib/exportUtils.ts (完整读取现有实现，了解 jsPDF 调用方式)
    - frontend/src/pages/EvaluateResultPage.tsx (了解 exportRef 范围，即导出 div 的内容量)
  </read_first>
  <action>
完整重写 `frontend/src/lib/exportUtils.ts`，保持函数签名不变（captureElementAsImage 和 captureElementAsPDF 的参数和返回类型不变）：

```typescript
import html2canvas from 'html2canvas'
import { jsPDF } from 'jspdf'

/**
 * Capture a DOM element as PNG and trigger download.
 * Uses 2x scale for Retina quality.
 * @param element - DOM element to capture
 * @param filename - Download filename (without .png extension)
 */
export async function captureElementAsImage(
  element: HTMLElement,
  filename: string,
): Promise<void> {
  const canvas = await html2canvas(element, {
    scale: 2,
    backgroundColor: '#FFFFFF',
    useCORS: true,
    logging: false,
    windowWidth: element.scrollWidth,
    windowHeight: element.scrollHeight,
  })
  const link = document.createElement('a')
  link.download = `${filename}.png`
  link.href = canvas.toDataURL('image/png')
  link.click()
}

/**
 * Capture a DOM element as a multi-page PDF (A4 portrait) and trigger download.
 * Content is paginated: if the element is taller than one A4 page, additional
 * pages are added automatically. No content is truncated.
 *
 * @param element - DOM element to capture
 * @param filename - Download filename (without .pdf extension)
 * @param title - Report title displayed at top of first page
 */
export async function captureElementAsPDF(
  element: HTMLElement,
  filename: string,
  title: string,
): Promise<void> {
  const canvas = await html2canvas(element, {
    scale: 2,
    backgroundColor: '#FFFFFF',
    useCORS: true,
    logging: false,
    windowWidth: element.scrollWidth,
    windowHeight: element.scrollHeight,
  })

  const imgData = canvas.toDataURL('image/png')
  const pdf = new jsPDF('p', 'mm', 'a4')

  const pageWidth = pdf.internal.pageSize.getWidth()   // 210mm
  const pageHeight = pdf.internal.pageSize.getHeight() // 297mm
  const margin = 10
  const contentWidth = pageWidth - margin * 2

  // Title on first page
  pdf.setFontSize(11)
  pdf.setTextColor(60, 60, 60)
  pdf.text(title, margin, margin + 5)

  const titleAreaHeight = 14  // mm reserved for title

  // Calculate image dimensions to fit page width
  const imgWidthMm = contentWidth
  const imgHeightMm = (canvas.height / canvas.width) * imgWidthMm

  // Available content height per page (first page has title)
  const firstPageContentHeight = pageHeight - margin * 2 - titleAreaHeight
  const otherPageContentHeight = pageHeight - margin * 2

  // Convert mm to canvas pixel ratio for slicing
  // canvas pixel per mm = canvas.width / imgWidthMm
  const pxPerMm = canvas.width / imgWidthMm

  let remainingHeightMm = imgHeightMm
  let canvasOffsetPx = 0
  let isFirstPage = true

  while (remainingHeightMm > 0) {
    const availableHeightMm = isFirstPage ? firstPageContentHeight : otherPageContentHeight
    const sliceHeightMm = Math.min(remainingHeightMm, availableHeightMm)
    const sliceHeightPx = Math.round(sliceHeightMm * pxPerMm)

    // Create a temporary canvas for this page slice
    const sliceCanvas = document.createElement('canvas')
    sliceCanvas.width = canvas.width
    sliceCanvas.height = sliceHeightPx
    const ctx = sliceCanvas.getContext('2d')
    if (ctx) {
      ctx.drawImage(
        canvas,
        0, canvasOffsetPx,                  // source x, y
        canvas.width, sliceHeightPx,        // source width, height
        0, 0,                               // dest x, y
        canvas.width, sliceHeightPx,        // dest width, height
      )
    }

    const sliceImgData = sliceCanvas.toDataURL('image/png')
    const yPos = isFirstPage ? margin + titleAreaHeight : margin

    pdf.addImage(sliceImgData, 'PNG', margin, yPos, imgWidthMm, sliceHeightMm)

    canvasOffsetPx += sliceHeightPx
    remainingHeightMm -= sliceHeightMm
    isFirstPage = false

    if (remainingHeightMm > 0.5) {  // 0.5mm tolerance to avoid blank trailing page
      pdf.addPage()
    }
  }

  pdf.save(`${filename}.pdf`)
}
```

注意：
- 函数签名与原版完全相同，现有调用方不需要修改
- html2canvas 添加 `windowWidth: element.scrollWidth` 和 `windowHeight: element.scrollHeight` 确保满载大元素完整截取
- PDF 分页逻辑：按 A4 高度切片，每片单独渲染到临时 canvas 再添加到 PDF
- 最后一页用 0.5mm 容差避免空白页
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "addPage\|sliceCanvas\|remainingHeightMm" /Users/slr/MiroFishmoody/frontend/src/lib/exportUtils.ts` 返回多行
    - `grep -n "windowWidth\|windowHeight" /Users/slr/MiroFishmoody/frontend/src/lib/exportUtils.ts` 返回 2 行（image 和 pdf 函数各 1 行）
    - `grep -n "Math.min(imgHeight" /Users/slr/MiroFishmoody/frontend/src/lib/exportUtils.ts` 返回 0 行（截断逻辑已删除）
    - `npm run build` 通过，无 TypeScript 错误
  </acceptance_criteria>
  <done>
    - exportUtils.ts 支持多页 PDF（内容超出 A4 时自动分页，不截断）
    - PNG 导出添加 windowWidth/windowHeight 确保满载截取完整
    - 函数签名不变，现有调用方零修改
  </done>
</task>

</tasks>

<verification>
1. `npm run build` 通过
2. `grep "addPage" /Users/slr/MiroFishmoody/frontend/src/lib/exportUtils.ts` 返回至少 1 行
3. `grep "truncat\|Math.min(imgHeight" /Users/slr/MiroFishmoody/frontend/src/lib/exportUtils.ts` 返回 0 行
</verification>

<success_criteria>
- captureElementAsPDF 实现多页分页，不截断 5 方案满载内容
- captureElementAsImage 添加 windowWidth/windowHeight 确保大页面完整截取
- npm run build 通过，无 TypeScript 错误
</success_criteria>

<output>
After completion, create `/Users/slr/MiroFishmoody/.planning/phases/14-frontend-rewrite-core-pages/14-04-SUMMARY.md`
</output>
