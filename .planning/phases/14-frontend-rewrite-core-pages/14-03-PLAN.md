---
phase: 14-frontend-rewrite-core-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/StepIndicator.tsx
  - frontend/src/components/SplitPanel.tsx
  - frontend/src/components/LogBuffer.tsx
  - frontend/src/pages/RunningPage.tsx
  - frontend/src/pages/EvaluatePage.tsx
autonomous: true
requirements:
  - FE-05
  - FE-07
must_haves:
  truths:
    - "RunningPage（Race path）显示横向步骤条（3 步），当前步骤高亮，左侧显示进度面板，右侧显示日志流"
    - "EvaluatePage（Evaluate path）在现有进度条上方，显示横向步骤条（3 步），当前步骤与 progress 百分比联动"
    - "LogBuffer 每 2 秒刷新一次日志，最多保留 200 条，自动滚到底部"
  artifacts:
    - path: "frontend/src/components/StepIndicator.tsx"
      provides: "Horizontal step indicator component (数字圆圈 + 连线 + 标签)"
      exports: ["StepIndicator"]
    - path: "frontend/src/components/SplitPanel.tsx"
      provides: "Left-right split panel with motion animation"
      exports: ["SplitPanel"]
    - path: "frontend/src/components/LogBuffer.tsx"
      provides: "Auto-scrolling log buffer component with 2s polling"
      exports: ["LogBuffer"]
    - path: "frontend/src/pages/RunningPage.tsx"
      provides: "Race running page with SplitPanel + StepIndicator"
      contains: "StepIndicator"
    - path: "frontend/src/pages/EvaluatePage.tsx"
      provides: "Evaluate running page with StepIndicator above progress bar"
      contains: "StepIndicator"
  key_links:
    - from: "EvaluatePage.tsx progress state"
      to: "StepIndicator currentStep prop"
      via: "getCurrentStageIndex(progress) maps to step index 0-2"
    - from: "LogBuffer component"
      to: "GET /api/task/{taskId}/logs (or message field from poll)"
      via: "2s setInterval poll, appends new messages, trims to 200 lines"
---

<objective>
新增 StepIndicator / SplitPanel / LogBuffer 三个可复用组件，并在 RunningPage 和 EvaluatePage 中使用。

Purpose: 推演进行中用户看到结构化的进度步骤（而非空转 spinner），以及日志流（透明感）。
Output: 3 个新组件文件 + 修改 RunningPage.tsx + EvaluatePage.tsx，npm run build 通过。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/slr/MiroFishmoody/.planning/PROJECT.md
@/Users/slr/MiroFishmoody/.planning/STATE.md
@/Users/slr/MiroFishmoody/CLAUDE.md

<interfaces>
<!-- Key contracts extracted from codebase. -->

From frontend/src/lib/api.ts (FROZEN):
```typescript
// TaskStatusResponse — shape returned by getEvaluateStatus(taskId)
export type TaskStatusResponse = {
  status: 'pending' | 'processing' | 'completed' | 'failed'
  progress: number   // 0-100
  message: string    // current status message, used as log line
  error?: string
}

// getRaceState() returns RaceState | null
// raceState.payload.plans.length = number of plans being processed
```

From EvaluatePage.tsx (existing stage definitions):
```typescript
const EVAL_STAGES = [
  { key: 'panel', label: 'Audience Panel 评审', range: [0, 40] as const },
  { key: 'pairwise', label: 'Pairwise 对决', range: [40, 80] as const },
  { key: 'scoring', label: '综合评分', range: [80, 90] as const },
  { key: 'summary', label: '总结报告', range: [90, 100] as const },
]
// Currently 4 stages — StepIndicator uses 3 steps mapped from these 4
// Step 0: 方案解析 (0-40%), Step 1: 评审分析 (40-80%), Step 2: 结果汇总 (80-100%)
```

From RunningPage.tsx (current state):
```typescript
// RunningPage currently shows just a spinner + fires raceCampaigns()
// No progress polling — Race completes synchronously (awaits result directly)
// SplitPanel left: step indicator + campaign count, right: simple log
```

motion 12.x is installed (package.json confirmed):
```typescript
import { motion } from 'motion/react'
// animate={{ width }}, transition cubic-bezier [0.4, 0, 0.2, 1]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 StepIndicator / SplitPanel / LogBuffer 组件</name>
  <files>
    frontend/src/components/StepIndicator.tsx
    frontend/src/components/SplitPanel.tsx
    frontend/src/components/LogBuffer.tsx
  </files>
  <read_first>
    - frontend/src/pages/EvaluatePage.tsx (了解现有 EVAL_STAGES 结构和 getCurrentStageIndex)
    - frontend/src/components/RadarScoreChart.tsx (参考现有组件文件结构规范)
  </read_first>
  <action>
**文件 1: `frontend/src/components/StepIndicator.tsx`**

横向步骤条，数字圆圈 + 横线连接 + 标签。Props：steps（步骤名称数组），currentStep（当前步骤 index，0-based）。

```typescript
// frontend/src/components/StepIndicator.tsx

type StepIndicatorProps = {
  steps: string[]
  currentStep: number  // 0-based index of active step
}

export function StepIndicator({ steps, currentStep }: StepIndicatorProps) {
  return (
    <div className="flex items-center w-full">
      {steps.map((label, i) => {
        const isDone = i < currentStep
        const isActive = i === currentStep
        return (
          <div key={i} className="flex items-center flex-1 last:flex-none">
            {/* Step circle */}
            <div className="flex flex-col items-center gap-1.5 shrink-0">
              <div
                className={`w-8 h-8 rounded-full flex items-center justify-center text-sm font-mono font-semibold transition-all duration-500 ${
                  isDone
                    ? 'bg-accent text-accent-foreground'
                    : isActive
                      ? 'bg-primary text-primary-foreground ring-4 ring-primary/20'
                      : 'bg-primary/20 text-primary-foreground/40'
                }`}
              >
                {isDone ? '✓' : i + 1}
              </div>
              <span
                className={`text-[11px] font-mono whitespace-nowrap transition-all duration-300 ${
                  isActive
                    ? 'text-primary-foreground font-semibold'
                    : isDone
                      ? 'text-primary-foreground/50'
                      : 'text-primary-foreground/25'
                }`}
              >
                {label}
              </span>
            </div>
            {/* Connector line between steps */}
            {i < steps.length - 1 && (
              <div className="flex-1 h-px mx-2 mt-[-20px] relative overflow-hidden bg-primary/20">
                <div
                  className="h-full bg-accent transition-all duration-700"
                  style={{ width: isDone ? '100%' : '0%' }}
                />
              </div>
            )}
          </div>
        )
      })}
    </div>
  )
}
```

**文件 2: `frontend/src/components/SplitPanel.tsx`**

左右分割面板，left 区域宽度固定 40%，right 区域占 60%。用 motion 动画实现 right 区域宽度缓入。

```typescript
// frontend/src/components/SplitPanel.tsx
import { motion } from 'motion/react'
import type { ReactNode } from 'react'

type SplitPanelProps = {
  left: ReactNode
  right: ReactNode
  className?: string
}

export function SplitPanel({ left, right, className = '' }: SplitPanelProps) {
  return (
    <div className={`flex gap-6 w-full ${className}`}>
      <div className="w-2/5 shrink-0">
        {left}
      </div>
      <motion.div
        className="flex-1 min-w-0"
        initial={{ width: 0, opacity: 0 }}
        animate={{ width: '60%', opacity: 1 }}
        transition={{ duration: 0.6, ease: [0.4, 0, 0.2, 1] }}
      >
        {right}
      </motion.div>
    </div>
  )
}
```

**文件 3: `frontend/src/components/LogBuffer.tsx`**

接收 messages 数组（字符串列表），自动滚到底部，最多显示 200 条。父组件负责传入 messages。

```typescript
// frontend/src/components/LogBuffer.tsx
import { useEffect, useRef } from 'react'

type LogBufferProps = {
  messages: string[]
  maxLines?: number
  className?: string
}

export function LogBuffer({ messages, maxLines = 200, className = '' }: LogBufferProps) {
  const scrollRef = useRef<HTMLDivElement>(null)
  const visibleMessages = messages.slice(-maxLines)

  useEffect(() => {
    const el = scrollRef.current
    if (!el) return
    el.scrollTop = el.scrollHeight
  }, [messages])

  return (
    <div
      ref={scrollRef}
      className={`overflow-y-auto font-mono text-[11px] text-primary-foreground/50 space-y-1 ${className}`}
    >
      {visibleMessages.length === 0 ? (
        <span className="text-primary-foreground/25">等待日志...</span>
      ) : (
        visibleMessages.map((msg, i) => (
          <div key={i} className="leading-relaxed">
            <span className="text-primary-foreground/25 mr-2">{String(visibleMessages.length - visibleMessages.length + i + 1).padStart(3, '0')}</span>
            {msg}
          </div>
        ))
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls /Users/slr/MiroFishmoody/frontend/src/components/StepIndicator.tsx` 文件存在
    - `ls /Users/slr/MiroFishmoody/frontend/src/components/SplitPanel.tsx` 文件存在
    - `ls /Users/slr/MiroFishmoody/frontend/src/components/LogBuffer.tsx` 文件存在
    - `grep -n "export function StepIndicator" /Users/slr/MiroFishmoody/frontend/src/components/StepIndicator.tsx` 返回 1 行
    - `grep -n "motion/react" /Users/slr/MiroFishmoody/frontend/src/components/SplitPanel.tsx` 返回 1 行
    - `npm run build` 通过
  </acceptance_criteria>
  <done>StepIndicator / SplitPanel / LogBuffer 三个组件文件创建，TypeScript 通过，可被其他文件 import</done>
</task>

<task type="auto">
  <name>Task 2: RunningPage + EvaluatePage 接入新组件</name>
  <files>
    frontend/src/pages/RunningPage.tsx
    frontend/src/pages/EvaluatePage.tsx
  </files>
  <read_first>
    - frontend/src/pages/RunningPage.tsx (完整读取现有实现)
    - frontend/src/pages/EvaluatePage.tsx (完整读取，了解现有 EVAL_STAGES 和 progress 状态)
    - frontend/src/components/StepIndicator.tsx (Task 1 刚创建)
    - frontend/src/components/SplitPanel.tsx (Task 1 刚创建)
    - frontend/src/components/LogBuffer.tsx (Task 1 刚创建)
  </read_first>
  <action>
**修改 1: `frontend/src/pages/RunningPage.tsx`**

RunningPage 目前 Race 路径无进度轮询（等待 raceCampaigns 同步返回）。改造为 SplitPanel 布局：

在现有 return JSX 中，将原来的 spinner + loader 替换为：

1. 在文件顶部 import 添加：
```typescript
import { StepIndicator } from '../components/StepIndicator'
import { SplitPanel } from '../components/SplitPanel'
import { LogBuffer } from '../components/LogBuffer'
```

2. 添加 useState 用于日志：
```typescript
const [logs, setLogs] = useState<string[]>([])
```

3. 在 raceCampaigns 调用的 .then 之前，可以给 logs 添加初始消息：
```typescript
// 在 startedRef guard 之后、raceCampaigns 之前添加：
setLogs(['正在提交推演任务...'])
```

4. 将正常状态（非 error）的 return JSX 改为：

```tsx
// 3-step Race flow: 提交任务 → 推演计算 → 生成结果
const RACE_STEPS = ['提交任务', '推演计算', '生成结果']
// Race is synchronous: once we hit this view we're at step 1 (index 1 = 推演计算)
const raceCurrentStep = 1

return (
  <div className="min-h-screen bg-primary text-primary-foreground flex flex-col items-center justify-center p-6">
    <div className="w-full max-w-2xl space-y-10">
      <div className="space-y-2 text-center">
        <h2 className="font-display text-4xl font-semibold">推演进行中</h2>
        <p className="text-primary-foreground/60 text-sm font-mono uppercase tracking-wider">
          正在处理 {getRaceState()?.payload?.plans?.length || 0} 个策略方向
        </p>
      </div>

      <StepIndicator steps={RACE_STEPS} currentStep={raceCurrentStep} />

      <SplitPanel
        left={
          <div className="space-y-4">
            <div className="text-xs font-mono text-primary-foreground/40 uppercase tracking-wider">进度</div>
            <div className="flex flex-col items-center gap-3">
              <Loader2 className="h-8 w-8 animate-spin text-primary-foreground/80" />
              <p className="text-primary-foreground/50 text-xs font-mono text-center">
                推演通常需要 10-20 秒
              </p>
            </div>
          </div>
        }
        right={
          <div className="space-y-2 h-32">
            <div className="text-xs font-mono text-primary-foreground/40 uppercase tracking-wider">日志</div>
            <LogBuffer messages={logs} className="h-24" />
          </div>
        }
      />

      <div className="text-center">
        <p className="text-xs text-primary-foreground/30 uppercase tracking-widest">请勿关闭此窗口</p>
      </div>
    </div>
  </div>
)
```

**修改 2: `frontend/src/pages/EvaluatePage.tsx`**

在现有 EVAL_STAGES 阶段列表上方插入 StepIndicator，映射到 3 步：

1. 在文件顶部 import 添加：
```typescript
import { StepIndicator } from '../components/StepIndicator'
import { LogBuffer } from '../components/LogBuffer'
```

2. 在组件内添加 logs 状态：
```typescript
const [logs, setLogs] = useState<string[]>([])
```

3. 在 setMessage 调用处同时追加到 logs：
```typescript
// 在 setMessage(res.message) 之后添加：
if (res.message) {
  setLogs(prev => [...prev, res.message])
}
```

4. 定义 3 步映射（在组件外部或组件内）：
```typescript
const STEP_LABELS = ['方案解析', '评审分析', '结果汇总']
// progress 0-39 = step 0, 40-79 = step 1, 80-100 = step 2
function progressToStep(p: number): number {
  if (p < 40) return 0
  if (p < 80) return 1
  return 2
}
```

5. 在现有 return JSX 内，在 `<div className="space-y-4">` (EVAL_STAGES 列表) 之前插入：
```tsx
<StepIndicator steps={STEP_LABELS} currentStep={progressToStep(progress)} />
```

6. 在现有进度条和 message 之后（`{message && ...}` 之后），添加 LogBuffer：
```tsx
{logs.length > 0 && (
  <div className="mt-4 h-24">
    <LogBuffer messages={logs} className="h-24" />
  </div>
)}
```

**保留所有现有逻辑，不删除 EVAL_STAGES 列表、进度条、error 处理等现有内容。**
  </action>
  <verify>
    <automated>cd /Users/slr/MiroFishmoody && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "StepIndicator" /Users/slr/MiroFishmoody/frontend/src/pages/RunningPage.tsx` 返回 import 行和使用行
    - `grep -n "StepIndicator" /Users/slr/MiroFishmoody/frontend/src/pages/EvaluatePage.tsx` 返回 import 行和使用行
    - `grep -n "SplitPanel" /Users/slr/MiroFishmoody/frontend/src/pages/RunningPage.tsx` 返回使用行
    - `grep -n "LogBuffer" /Users/slr/MiroFishmoody/frontend/src/pages/EvaluatePage.tsx` 返回使用行
    - `npm run build` 通过，无 TypeScript 错误
  </acceptance_criteria>
  <done>
    - RunningPage 显示 SplitPanel + StepIndicator（3 步，当前高亮"推演计算"）
    - EvaluatePage 在 EVAL_STAGES 列表上方显示 StepIndicator，步骤与 progress 百分比联动
    - 两个页面均有 LogBuffer 显示消息流
  </done>
</task>

</tasks>

<verification>
1. `npm run build` 通过
2. `ls /Users/slr/MiroFishmoody/frontend/src/components/StepIndicator.tsx /Users/slr/MiroFishmoody/frontend/src/components/SplitPanel.tsx /Users/slr/MiroFishmoody/frontend/src/components/LogBuffer.tsx` 全部存在
3. `grep -rn "StepIndicator" /Users/slr/MiroFishmoody/frontend/src/pages/` 返回 RunningPage 和 EvaluatePage 各有使用
</verification>

<success_criteria>
- 3 个新组件创建完毕，TypeScript strict 通过
- RunningPage 使用 SplitPanel + StepIndicator + LogBuffer
- EvaluatePage 在进度条上方使用 StepIndicator（3 步映射），同时保留原有 EVAL_STAGES 列表
- npm run build 通过
</success_criteria>

<output>
After completion, create `/Users/slr/MiroFishmoody/.planning/phases/14-frontend-rewrite-core-pages/14-03-SUMMARY.md`
</output>
