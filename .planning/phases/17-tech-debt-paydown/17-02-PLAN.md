---
phase: 17-tech-debt-paydown
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_brand_state_characterization.py
  - backend/app/services/backtest_engine.py
  - backend/app/services/brand_state_engine.py
autonomous: true
requirements:
  - TD-02

must_haves:
  truths:
    - "BrandStateEngine 全部公开方法有 characterization tests，pytest 绿灯"
    - "BacktestEngine 是独立文件，包含从 BrandStateEngine 提取的 backtest() 方法"
    - "BrandStateEngine.backtest() 保留（委托给 BacktestEngine），现有测试不变"
  artifacts:
    - path: "backend/tests/test_brand_state_characterization.py"
      provides: "BrandStateEngine 所有公开方法的 characterization tests"
      exports: []
    - path: "backend/app/services/backtest_engine.py"
      provides: "BacktestEngine 独立类，含 backtest() 方法"
      exports: ["BacktestEngine"]
    - path: "backend/app/services/brand_state_engine.py"
      provides: "BrandStateEngine.backtest() 委托给 BacktestEngine"
  key_links:
    - from: "brand_state_engine.py:BrandStateEngine.backtest()"
      to: "backtest_engine.py:BacktestEngine.backtest()"
      via: "delegation / forwarding call"
      pattern: "BacktestEngine"
---

<objective>
为 BrandStateEngine 写 characterization tests（捕获现有行为，防止重构破坏），然后将 backtest() 方法用 strangler fig 模式提取为独立 BacktestEngine 类。

Purpose: TD-02 是 BrandStateEngine 渐进分解的第一刀——先有测试保护网，再安全切割。
Output: characterization test 文件 + BacktestEngine 独立类 + BrandStateEngine 保留委托引用。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/slr/MiroFishmoody/.planning/PROJECT.md
@/Users/slr/MiroFishmoody/.planning/ROADMAP.md
@/Users/slr/MiroFishmoody/.planning/STATE.md

<interfaces>
<!-- BrandStateEngine public methods (from brand_state_engine.py) -->

```python
class BrandStateEngine:
    def __init__(self, store: BrandictionStore = None): ...

    # Public methods to characterize:
    def build_state_from_signals(self, as_of_date, product_line, audience_segment, use_decay, market) -> BrandState
    def compute_intervention_impact(self, intervention, outcomes, market) -> Dict[str, float]
    def replay_history(self, product_line, audience_segment, initial_state, market) -> List[BrandState]
    def predict_impact(self, intervention_plan, base_state, product_line, audience_segment, market) -> Dict
    def predict_with_diffusion(self, intervention_plan, base_state, product_line, audience_segment, market) -> Dict
    def cognition_probability_board(self, product_line, audience_segment, market) -> Dict
    def simulate_scenario(self, steps, product_line, audience_segment, base_state, scenario_id, market) -> Dict
    def compare_scenarios(self, scenarios, product_line, audience_segment, market) -> Dict
    def backtest(self, product_line, audience_segment, market) -> Dict
```

<!-- Existing test infrastructure in tests/test_brand_state.py -->

```python
def _fresh_store():
    BrandictionStore._reset_instance()
    fd, path = tempfile.mkstemp(suffix=".db")
    os.close(fd)
    store = BrandictionStore(db_path=path)
    return store, path

def _cleanup(store, path):
    BrandictionStore._reset_instance()
    try:
        os.unlink(path)
    except OSError:
        pass
```

<!-- BacktestEngine extraction target (lines 1137-1270 of brand_state_engine.py) -->
<!-- backtest() reads from self.store — BacktestEngine needs store injected via __init__ -->
<!-- Also uses: self.compute_intervention_impact(), self._inject_competitor_delta() — these stay in BrandStateEngine for now -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 写 BrandStateEngine characterization tests</name>
  <files>backend/tests/test_brand_state_characterization.py</files>
  <read_first>
    backend/app/services/brand_state_engine.py (lines 207-235, 314-445, 467-558, 628-795, 822-919, 919-1135, 1137-1272)
    backend/tests/test_brand_state.py (lines 1-80, 全部 fixture 模式)
    backend/app/services/brandiction_store.py (只看 class BrandictionStore 的 public methods)
  </read_first>
  <action>
    创建 `backend/tests/test_brand_state_characterization.py`，使用 _fresh_store() 模式。

    为以下每个公开方法写至少 1 个 characterization test（捕获现有行为，不驱动新功能）：

    1. **build_state_from_signals** — 空 store 时返回 BrandState，dimensions 为空 dict。
       ```python
       def test_build_state_from_signals_empty_store():
           store, path = _fresh_store()
           try:
               engine = BrandStateEngine(store)
               state = engine.build_state_from_signals("2025-01-01")
               assert state.product_line == "moodyplus"
               assert isinstance(state.perception, PerceptionVector)
           finally:
               _cleanup(store, path)
       ```

    2. **compute_intervention_impact** — 给定 HistoricalIntervention（无 outcomes），返回 dict，keys 为 PERCEPTION_DIMENSIONS，values 为 float。
       ```python
       def test_compute_intervention_impact_no_outcomes():
           store, path = _fresh_store()
           try:
               engine = BrandStateEngine(store)
               iv = HistoricalIntervention(
                   intervention_id="t1", run_id="r1",
                   theme="science", product_line="moodyplus",
                   channel_mix=["bilibili"], budget=50000,
               )
               delta = engine.compute_intervention_impact(iv, [], market="cn")
               assert set(delta.keys()) == set(PERCEPTION_DIMENSIONS)
               assert all(isinstance(v, float) for v in delta.values())
           finally:
               _cleanup(store, path)
       ```

    3. **replay_history** — 空 store 时返回空 list。
       ```python
       def test_replay_history_empty_store():
           store, path = _fresh_store()
           try:
               engine = BrandStateEngine(store)
               result = engine.replay_history()
               assert result == []
           finally:
               _cleanup(store, path)
       ```

    4. **predict_impact** — 空 store，给定 theme，返回 dict 含 "current_state"、"delta"、"confidence" 字段。
       ```python
       def test_predict_impact_returns_required_fields():
           store, path = _fresh_store()
           try:
               engine = BrandStateEngine(store)
               result = engine.predict_impact({"theme": "science"})
               assert "current_state" in result
               assert "delta" in result
               assert "confidence" in result
           finally:
               _cleanup(store, path)
       ```

    5. **cognition_probability_board** — 空 store 时返回 dict，含 "campaigns" key（可能为空 list）。
       ```python
       def test_cognition_probability_board_empty_store():
           store, path = _fresh_store()
           try:
               engine = BrandStateEngine(store)
               result = engine.cognition_probability_board()
               assert isinstance(result, dict)
               assert "campaigns" in result
           finally:
               _cleanup(store, path)
       ```

    6. **simulate_scenario** — 空 steps 时 raise ValueError（已有 guard）。
       ```python
       def test_simulate_scenario_empty_steps_raises():
           store, path = _fresh_store()
           try:
               engine = BrandStateEngine(store)
               with pytest.raises(ValueError):
                   engine.simulate_scenario([])
           finally:
               _cleanup(store, path)
       ```

    7. **backtest** — 空 store 时返回 dict 含 "total_interventions"=0, "tested"=0。
       ```python
       def test_backtest_empty_store():
           store, path = _fresh_store()
           try:
               engine = BrandStateEngine(store)
               result = engine.backtest()
               assert result["total_interventions"] == 0
               assert result["tested"] == 0
           finally:
               _cleanup(store, path)
       ```

    文件顶部 imports：
    ```python
    import sys, os, tempfile, pytest
    sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
    from app.services.brand_state_engine import BrandStateEngine
    from app.services.brandiction_store import BrandictionStore
    from app.models.brand_state import PERCEPTION_DIMENSIONS, PerceptionVector
    from app.models.brandiction import HistoricalIntervention
    ```

    def _fresh_store() 和 _cleanup() 复制自 test_brand_state.py（不导入，直接在文件内定义）。
  </action>
  <verify>
    cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_brand_state_characterization.py -v
  </verify>
  <acceptance_criteria>
    cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_brand_state_characterization.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"
    # 必须全部为 PASSED，0 FAILED，0 ERROR
  </acceptance_criteria>
  <done>7 个 characterization tests 全部 PASSED</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 提取 BacktestEngine（strangler fig 第一刀）</name>
  <files>
    backend/app/services/backtest_engine.py
    backend/app/services/brand_state_engine.py
  </files>
  <read_first>
    backend/app/services/brand_state_engine.py (lines 1-30 imports, 207-215 class init, 314-445 compute_intervention_impact, 558-627 _inject_competitor_delta, 1137-1272 backtest method)
    backend/tests/test_brand_state_characterization.py (刚写完，确认 backtest test 已通过)
  </read_first>
  <action>
    **Step 1: 创建 backend/app/services/backtest_engine.py**

    BacktestEngine 接受 store 注入，但 compute_intervention_impact 和 _inject_competitor_delta 两个 helper 仍在 BrandStateEngine — 通过构造函数注入 engine 引用（或直接注入两个 callable）来复用。

    推荐方案：注入 engine 实例（最简单，不需要复制 helper 方法）：

    ```python
    """
    BacktestEngine — 从 BrandStateEngine 提取的回测模块（TD-02 strangler fig）
    """
    from typing import Any, Dict, List, Optional
    from ..models.brand_state import PERCEPTION_DIMENSIONS
    from ..models.brandiction import HistoricalIntervention
    from .brandiction_store import BrandictionStore


    class BacktestEngine:
        """
        留一法回测引擎。
        依赖 BrandictionStore 读数据，依赖外部 engine 提供
        compute_intervention_impact 和 _inject_competitor_delta。
        """

        def __init__(self, store: BrandictionStore, engine):
            self.store = store
            self._engine = engine  # BrandStateEngine instance for helper methods

        def backtest(
            self,
            product_line: str = "moodyplus",
            audience_segment: str = "general",
            market: str = "cn",
        ) -> Dict[str, Any]:
            # 复制 BrandStateEngine.backtest() 的完整逻辑（逐行复制，不修改）
            # 将所有 self.store -> self.store
            # 将所有 self.compute_intervention_impact -> self._engine.compute_intervention_impact
            # 将所有 self._inject_competitor_delta -> self._engine._inject_competitor_delta
            ...
    ```

    将 BrandStateEngine.backtest()（lines 1137-1270）的完整逻辑复制进 BacktestEngine.backtest()，
    仅做以下三处替换：
    - `self.store.` → `self.store.`（不变）
    - `self.compute_intervention_impact(` → `self._engine.compute_intervention_impact(`
    - `self._inject_competitor_delta(` → `self._engine._inject_competitor_delta(`

    **Step 2: 修改 brand_state_engine.py 中的 backtest() 为委托**

    将 BrandStateEngine.backtest() 方法体替换为委托调用：

    ```python
    def backtest(
        self,
        product_line: str = "moodyplus",
        audience_segment: str = "general",
        market: str = "cn",
    ) -> Dict[str, Any]:
        """
        留一法回测。委托给 BacktestEngine（TD-02 strangler fig）。
        """
        from .backtest_engine import BacktestEngine
        bt = BacktestEngine(self.store, self)
        return bt.backtest(product_line=product_line, audience_segment=audience_segment, market=market)
    ```

    注意：
    - 使用 lazy import（`from .backtest_engine import BacktestEngine` 在方法内）避免循环导入
    - 原方法签名、docstring 风格保持一致
    - 不修改 BrandStateEngine 其他任何方法
  </action>
  <verify>
    cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_brand_state_characterization.py tests/test_brand_state.py tests/test_brandiction.py -v -q
  </verify>
  <acceptance_criteria>
    # BacktestEngine 文件存在
    ls /Users/slr/MiroFishmoody/backend/app/services/backtest_engine.py

    # BacktestEngine 类定义存在
    grep -n "class BacktestEngine" /Users/slr/MiroFishmoody/backend/app/services/backtest_engine.py

    # BrandStateEngine.backtest() 委托给 BacktestEngine
    grep -n "BacktestEngine" /Users/slr/MiroFishmoody/backend/app/services/brand_state_engine.py

    # characterization test 中的 backtest test 仍然 PASSED（通过委托路径）
    cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_brand_state_characterization.py::test_backtest_empty_store -v
  </acceptance_criteria>
  <done>
    backtest_engine.py 存在并含 BacktestEngine 类；BrandStateEngine.backtest() 委托给它；
    全部相关 pytest 绿灯
  </done>
</task>

</tasks>

<verification>
全量 pytest 验证（确保现有测试不受影响）：
```
cd /Users/slr/MiroFishmoody/backend && uv run pytest -x -q
```

结构验证：
```
grep -n "class BacktestEngine" /Users/slr/MiroFishmoody/backend/app/services/backtest_engine.py
grep -n "BacktestEngine" /Users/slr/MiroFishmoody/backend/app/services/brand_state_engine.py
```
</verification>

<success_criteria>
1. test_brand_state_characterization.py 存在，7 个 tests 全部 PASSED
2. backtest_engine.py 存在，包含 BacktestEngine 类和 backtest() 方法
3. brand_state_engine.py 中 backtest() 委托给 BacktestEngine（grep 可验证）
4. `uv run pytest -x -q` 全绿，0 新增失败
</success_criteria>

<output>
完成后创建 `.planning/phases/17-tech-debt-paydown/17-02-SUMMARY.md`
</output>
