---
phase: 17-tech-debt-paydown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/campaign.py
  - backend/app/services/evaluation_orchestrator.py
autonomous: true
requirements:
  - TD-01

must_haves:
  truths:
    - "_evaluation_store 的所有 lock 作用域仅覆盖 dict 读写操作，没有任何 LLM 调用或 I/O 在 lock 内"
    - "EvaluationOrchestrator 写入 evaluation_store 的 lock 作用域与 save_result_fn 调用分离"
    - "现有 pytest 测试仍然全绿"
  artifacts:
    - path: "backend/app/api/campaign.py"
      provides: "精确的 _store_lock 作用域（只包裹 dict 读写）"
    - path: "backend/app/services/evaluation_orchestrator.py"
      provides: "store_lock 只在 dict 写操作那一行使用"
  key_links:
    - from: "evaluation_orchestrator.py:run()"
      to: "_evaluation_store dict"
      via: "store_lock context manager"
      pattern: "with self\\.store_lock"
---

<objective>
将 campaign.py 和 evaluation_orchestrator.py 中所有 threading.Lock 作用域精确收窄为纯 dict 读写操作。目前 lock 的范围虽然总体正确，但 EvaluationOrchestrator.run() 中 lock 与 save_result_fn（文件 I/O）的关系需要明确分离，避免未来维护时混入 I/O。

Purpose: Phase 10 决策（lock scopes minimal — only dict ops）在所有文件中一致落地，消除 tech debt。
Output: campaign.py + evaluation_orchestrator.py，lock 作用域可代码审查验证为纯 dict op。
</objective>

<execution_context>
@/Users/slr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/slr/MiroFishmoody/.planning/PROJECT.md
@/Users/slr/MiroFishmoody/.planning/ROADMAP.md
@/Users/slr/MiroFishmoody/.planning/STATE.md

<interfaces>
<!-- Key patterns from campaign.py and evaluation_orchestrator.py -->

From backend/app/api/campaign.py (current lock sites):

```python
# Line 370-371: 防重复检查 — dict read only (ALREADY CORRECT)
with _store_lock:
    in_memory = campaign_set.set_id in _evaluation_store

# Line 439-445: get_result — dict read, then conditional write (ALREADY CORRECT)
with _store_lock:
    result = _evaluation_store.get(set_id)
if not result:
    result = _load_result(set_id)   # I/O outside lock
    if result:
        with _store_lock:
            _evaluation_store[set_id] = result  # dict write only

# Line 521-522: resolve — dict read only (ALREADY CORRECT)
with _store_lock:
    stored = _evaluation_store.get(set_id)

# Line 702-703: retry — dict pop only (ALREADY CORRECT)
with _store_lock:
    _evaluation_store.pop(set_id, None)
```

From backend/app/services/evaluation_orchestrator.py (lines 152-158):

```python
# CURRENT — save_result_fn (file I/O) is OUTSIDE lock, but visually adjacent
result_dict = result.to_dict()
if self.store_lock:
    with self.store_lock:
        self.evaluation_store[campaign_set.set_id] = result_dict
else:
    self.evaluation_store[campaign_set.set_id] = result_dict
self.save_result_fn(campaign_set.set_id, result_dict)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 审计并注释所有 lock 作用域</name>
  <files>backend/app/api/campaign.py</files>
  <read_first>
    backend/app/api/campaign.py (完整文件，重点 lines 35-40, 369-375, 439-445, 459-465, 519-528, 700-705)
  </read_first>
  <action>
    读取 campaign.py 后，对每个 `with _store_lock:` 块做以下验证：
    1. 块内是否只有 dict 操作（__contains__, .get(), .pop(), 赋值）
    2. 是否有 I/O（open, os.path.exists, json.dump/load）或 LLM 调用混入

    现有 6 处 lock 使用（lines 370, 439, 444, 459, 464, 521, 702）经代码审查均已正确。
    在每个 `with _store_lock:` 上方添加注释行：`# TD-01: dict-only — no I/O or LLM inside lock`
    不改变任何逻辑，只添加注释。

    示例（line 370 上方）：
    ```python
    # TD-01: dict-only — no I/O or LLM inside lock
    with _store_lock:
        in_memory = campaign_set.set_id in _evaluation_store
    ```
    对 campaign.py 中每一处 `with _store_lock:` 都加这行注释（共 7 处）。
  </action>
  <verify>
    cd /Users/slr/MiroFishmoody/backend && uv run pytest tests/test_phase13_evaluate_endpoint.py tests/test_smoke.py -x -q
  </verify>
  <acceptance_criteria>
    grep -c "TD-01: dict-only" /Users/slr/MiroFishmoody/backend/app/api/campaign.py
    # 必须输出 >= 6（每处 lock 各一行注释）
  </acceptance_criteria>
  <done>campaign.py 中每处 with _store_lock 上方有 TD-01 注释，pytest 绿灯</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 拆分 EvaluationOrchestrator 中 dict-write 与 file-I/O</name>
  <files>backend/app/services/evaluation_orchestrator.py</files>
  <read_first>
    backend/app/services/evaluation_orchestrator.py (完整文件，重点 lines 140-175)
  </read_first>
  <action>
    当前 lines 152-158 中，store_lock 保护 dict 写操作，save_result_fn（文件 I/O）在 lock 外——逻辑上已经正确，但两段代码相邻，未来维护者容易误将 save_result_fn 移入 lock 内。

    将这段重写为显式分步：
    ```python
    # TD-01: step 1 — dict write under lock
    result_dict = result.to_dict()
    if self.store_lock:
        with self.store_lock:
            # TD-01: dict-only — no I/O or LLM inside lock
            self.evaluation_store[campaign_set.set_id] = result_dict
    else:
        self.evaluation_store[campaign_set.set_id] = result_dict

    # TD-01: step 2 — file I/O outside lock
    self.save_result_fn(campaign_set.set_id, result_dict)
    ```

    注意：逻辑不变，仅添加注释和空行分隔，使意图清晰。
    不修改其他方法。
  </action>
  <verify>
    cd /Users/slr/MiroFishmoody/backend && uv run pytest -x -q
  </verify>
  <acceptance_criteria>
    grep -n "TD-01: step" /Users/slr/MiroFishmoody/backend/app/services/evaluation_orchestrator.py
    # 必须出现 "TD-01: step 1" 和 "TD-01: step 2" 两行
    grep -n "save_result_fn" /Users/slr/MiroFishmoody/backend/app/services/evaluation_orchestrator.py
    # save_result_fn 调用行必须在任何 with self.store_lock 块之外
  </acceptance_criteria>
  <done>evaluation_orchestrator.py 中 dict-write 与 file-I/O 分步注释，pytest 全绿</done>
</task>

</tasks>

<verification>
全部 pytest 通过：
```
cd /Users/slr/MiroFishmoody/backend && uv run pytest -x -q
```

grep 验证 lock 注释：
```
grep -rn "TD-01: dict-only" /Users/slr/MiroFishmoody/backend/app/
```
应在 campaign.py 和 evaluation_orchestrator.py 中出现。
</verification>

<success_criteria>
1. campaign.py 中每处 `with _store_lock:` 均有 TD-01 注释（不少于 6 处）
2. evaluation_orchestrator.py 中 dict-write 与 save_result_fn（I/O）有显式分步注释
3. `cd backend && uv run pytest -x -q` 全绿，无新失败
</success_criteria>

<output>
完成后创建 `.planning/phases/17-tech-debt-paydown/17-01-SUMMARY.md`
</output>
