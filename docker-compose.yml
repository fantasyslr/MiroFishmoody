services:
  campaign-ranker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: campaign-ranker
    env_file:
      - .env
    ports:
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/uploads
